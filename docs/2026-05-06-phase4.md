下面是 **Phase 4 详细执行计划**，可以直接复制给编码模型执行。

本 Phase 明确要求：

```text
去除所有需要大模型的功能。
不做 LLM 摘要。
不做 LLM 自动生成脑图。
不做 LLM 自动整理节点。
不做 LLM 智能布局。
不做 LLM 问答。
```

Phase 4 只做纯工程能力：

```text
1. 更强的局部避让布局
2. 大图性能优化增强
3. Canvas/SVG 混合渲染
4. 更精细的 semantic zoom 策略
5. notebook 内容预览缓存
6. 渲染稳定性与交互优化
```

---

# Phase 4：工程增强与性能优化执行计划

## 0. 当前项目状态假设

已经完成：

```text
Phase 1：
- text / notebook 节点
- SVG renderer
- semantic projection
- notebook preview
- radial layout

Phase 2：
- 拖拽
- Tab / Enter 新增节点
- 搜索
- 多选 / 框选
- reference 连线
- undo / redo

Phase 3：
- 设置页
- notebook folder / template
- 选择已有 notebook
- Markdown headings → mindmap
- backlinks / outlinks → local map
- weighted radial layout
- viewport culling
- debug overlay
```

Phase 4 不改变核心数据模型，只增强底层工程能力。

---

# 1. Phase 4 功能范围

## 1.1 要实现

```text
1. 更强的局部避让布局
2. 大图性能优化增强
3. Canvas/SVG 混合渲染
4. semantic zoom 策略细化
5. notebook preview 缓存优化
6. 交互稳定性优化
7. 渲染性能监控
8. Renderer 自动选择
```

---

## 1.2 不实现

```text
1. 不接入任何 LLM
2. 不做 AI 摘要
3. 不做 AI 自动生成脑图
4. 不做 AI 自动命名节点
5. 不做 AI 自动分类
6. 不做 AI 自动布局
7. 不做 AI 问答
8. 不做 AI 推荐关联节点
9. 不做 AI 生成 notebook 内容
10. 不做 AI 语义聚类
```

---

# 2. Phase 4 新增目录

继续使用现有目录，新增：

```text
src/
  core/
    layout-collision.ts
    layout-relaxation.ts
    render-mode.ts
    performance-monitor.ts
    preview-cache.ts
    semantic-zoom-policy.ts

  renderer/
    canvas-background-renderer.ts
    hybrid-mindmap-renderer.ts
    renderer-adapter.ts

  ui/
    performance-debug-overlay.ts
```

---

# 3. Renderer Adapter 抽象

Phase 4 要支持：

```text
SVG renderer
Hybrid renderer = Canvas 背景层 + SVG 交互层
```

所以先抽象 renderer。

创建：

```text
src/renderer/renderer-adapter.ts
```

```ts
export interface RendererAdapter {
  mount(): void;
  unmount(): void;
  render(): void;

  focusNode?(nodeId: string): void;
  setLastFocusNodeId?(nodeId: string): void;
  forceDetailLevel?(nodeId: string, level: number): void;

  setSearchResultIds?(ids: Set<string>): void;
  setConnectionState?(state: { enabled: boolean; sourceId?: string }): void;
}
```

修改：

```text
src/renderer/svg-mindmap-renderer.ts
```

让 `SvgMindmapRenderer` 实现：

```ts
import type { RendererAdapter } from "./renderer-adapter";

export class SvgMindmapRenderer implements RendererAdapter {
  // existing code
}
```

---

# 4. 渲染模式选择

创建：

```text
src/core/render-mode.ts
```

```ts
export type RenderMode = "svg" | "hybrid";

export interface RenderModePolicy {
  svgMaxNodes: number;
  hybridMinNodes: number;
}

export const DEFAULT_RENDER_MODE_POLICY: RenderModePolicy = {
  svgMaxNodes: 1200,
  hybridMinNodes: 1201,
};

export function chooseRenderMode(args: {
  nodeCount: number;
  edgeCount: number;
  policy?: RenderModePolicy;
}): RenderMode {
  const policy = args.policy ?? DEFAULT_RENDER_MODE_POLICY;

  if (args.nodeCount >= policy.hybridMinNodes) {
    return "hybrid";
  }

  return "svg";
}
```

规则：

```text
1. 小图：SVG 全量交互渲染
2. 大图：Canvas 绘制低细节背景，SVG 绘制交互节点
3. Renderer 选择只在打开文件或重新加载时发生
4. 不要每次 render 动态切换 renderer
```

---

# 5. 性能监控

创建：

```text
src/core/performance-monitor.ts
```

```ts
export interface RenderPerformanceSample {
  timestamp: number;
  durationMs: number;
  nodeCount: number;
  edgeCount: number;
  renderedNodeCount: number;
  renderedEdgeCount: number;
  mode: "svg" | "hybrid";
}

export class PerformanceMonitor {
  private samples: RenderPerformanceSample[] = [];

  constructor(private maxSamples = 120) {}

  measure<T>(
    sampleBase: Omit<RenderPerformanceSample, "timestamp" | "durationMs">,
    fn: () => T,
  ): T {
    const start = performance.now();

    const result = fn();

    const durationMs = performance.now() - start;

    this.push({
      ...sampleBase,
      timestamp: Date.now(),
      durationMs,
    });

    return result;
  }

  push(sample: RenderPerformanceSample): void {
    this.samples.push(sample);

    if (this.samples.length > this.maxSamples) {
      this.samples.shift();
    }
  }

  getLastSample(): RenderPerformanceSample | null {
    return this.samples[this.samples.length - 1] ?? null;
  }

  getAverageDuration(): number {
    if (this.samples.length === 0) return 0;

    return (
      this.samples.reduce((sum, sample) => sum + sample.durationMs, 0) /
      this.samples.length
    );
  }

  isSlow(): boolean {
    return this.getAverageDuration() > 32;
  }
}
```

---

# 6. Performance Debug Overlay

创建：

```text
src/ui/performance-debug-overlay.ts
```

```ts
import type { RenderPerformanceSample } from "../core/performance-monitor";

export class PerformanceDebugOverlay {
  private el: HTMLDivElement;

  constructor(container: HTMLElement) {
    this.el = container.createDiv({ cls: "mindmap-performance-debug-overlay" });
  }

  update(args: {
    sample: RenderPerformanceSample | null;
    averageDuration: number;
    isSlow: boolean;
  }): void {
    const sample = args.sample;

    if (!sample) {
      this.el.setText("No render stats");
      return;
    }

    this.el.setText(
      [
        `mode: ${sample.mode}`,
        `render: ${sample.durationMs.toFixed(1)}ms`,
        `avg: ${args.averageDuration.toFixed(1)}ms`,
        `nodes: ${sample.renderedNodeCount}/${sample.nodeCount}`,
        `edges: ${sample.renderedEdgeCount}/${sample.edgeCount}`,
        args.isSlow ? "slow" : "ok",
      ].join(" | "),
    );
  }

  remove(): void {
    this.el.remove();
  }
}
```

CSS：

```css
.mindmap-performance-debug-overlay {
  position: absolute;
  right: 12px;
  bottom: 12px;
  z-index: 20;
  padding: 4px 8px;
  border-radius: 6px;
  background: var(--background-secondary);
  color: var(--text-muted);
  font-size: 11px;
  pointer-events: none;
}
```

---

# 7. 更精细的 Semantic Zoom Policy

当前 detailLevel 规则较粗。Phase 4 把策略抽出来。

创建：

```text
src/core/semantic-zoom-policy.ts
```

```ts
import type { NodeDetailLevel, NodeKind } from "../types/mindmap";
import { clampDetailLevel, zoomToBaseDetailLevel } from "./detail-level";

export interface SemanticZoomPolicyInput {
  zoom: number;
  kind: NodeKind;

  isRoot: boolean;
  isFocus: boolean;
  isSelected: boolean;
  isHovered: boolean;
  isAncestorPath: boolean;

  hasNotebook: boolean;
  hasChildren: boolean;

  distanceToFocus: number;
}

export function computeSemanticDetailLevel(
  input: SemanticZoomPolicyInput,
): NodeDetailLevel {
  let level: NodeDetailLevel = zoomToBaseDetailLevel(input.zoom);

  /**
   * 普通 text 节点不要无限变大。
   */
  if (input.kind === "text") {
    level = clampDetailLevel(Math.min(level, 2));
  }

  /**
   * notebook 节点允许到 L5。
   */
  if (input.kind === "notebook") {
    level = zoomToBaseDetailLevel(input.zoom);
  }

  /**
   * 非焦点远端降级。
   */
  if (
    !input.isRoot &&
    !input.isFocus &&
    !input.isSelected &&
    !input.isHovered &&
    !input.isAncestorPath
  ) {
    if (input.distanceToFocus > 900) {
      level = clampDetailLevel(level - 2);
    } else if (input.distanceToFocus > 450) {
      level = clampDetailLevel(level - 1);
    }
  }

  /**
   * ancestor path 最低 L1。
   */
  if (input.isAncestorPath) {
    level = clampDetailLevel(Math.max(level, 1));
  }

  /**
   * root 最低 L1。
   */
  if (input.isRoot) {
    level = clampDetailLevel(Math.max(level, 1));
  }

  /**
   * focus / selected / hovered 最低 L2。
   */
  if (input.isFocus || input.isSelected || input.isHovered) {
    level = clampDetailLevel(Math.max(level, 2));
  }

  /**
   * text 节点即使 selected，也不超过 L2。
   */
  if (input.kind === "text") {
    level = clampDetailLevel(Math.min(level, 2));
  }

  return level;
}
```

---

## 7.1 semantic-projection.ts 接入

修改：

```text
src/core/semantic-projection.ts
```

import：

```ts
import { computeSemanticDetailLevel } from "./semantic-zoom-policy";
```

在计算 detailLevel 的位置，替换原逻辑。

先计算 focus node：

```ts
const focusNode = focusNodeId
  ? doc.nodes.find((node) => node.id === focusNodeId)
  : undefined;
```

添加距离函数：

```ts
function distance(a: { x: number; y: number }, b: { x: number; y: number }): number {
  const dx = a.x - b.x;
  const dy = a.y - b.y;
  return Math.sqrt(dx * dx + dy * dy);
}
```

循环里：

```ts
const distanceToFocus = focusNode
  ? distance(node, focusNode)
  : 0;

const detail = computeSemanticDetailLevel({
  zoom: context.zoom,
  kind: node.kind,

  isRoot,
  isFocus,
  isSelected,
  isHovered,
  isAncestorPath,

  hasNotebook: Boolean(node.notebook),
  hasChildren: children.length > 0,

  distanceToFocus,
});
```

注意：

```text
text 节点不显示 preview。
notebook 节点才允许 L5 preview。
```

---

# 8. 更强的局部避让布局

当前 `resolveSimpleOverlap` 太粗。Phase 4 增加局部 relaxation。

创建：

```text
src/core/layout-collision.ts
```

```ts
import type { ProjectedNode, Rect } from "../types/mindmap";

export function projectedNodeRect(node: ProjectedNode): Rect {
  return {
    x: node.projectedX,
    y: node.projectedY,
    width: node.displayWidth,
    height: node.displayHeight,
  };
}

export function rectsOverlap(a: Rect, b: Rect, padding = 12): boolean {
  return !(
    a.x + a.width + padding < b.x ||
    b.x + b.width + padding < a.x ||
    a.y + a.height + padding < b.y ||
    b.y + b.height + padding < a.y
  );
}

export function overlapVector(a: Rect, b: Rect): { x: number; y: number } {
  const ax = a.x + a.width / 2;
  const ay = a.y + a.height / 2;
  const bx = b.x + b.width / 2;
  const by = b.y + b.height / 2;

  const dx = ax - bx || 1;
  const dy = ay - by || 1;

  const length = Math.sqrt(dx * dx + dy * dy);

  return {
    x: dx / length,
    y: dy / length,
  };
}
```

创建：

```text
src/core/layout-relaxation.ts
```

```ts
import type { ProjectedNode } from "../types/mindmap";
import { overlapVector, projectedNodeRect, rectsOverlap } from "./layout-collision";

export interface RelaxProjectionOptions {
  iterations?: number;
  pushStrength?: number;
  maxMovePerIteration?: number;
}

/**
 * 只调整 projectedX / projectedY。
 * 不写回 JSON。
 */
export function relaxProjectedNodes(
  nodes: ProjectedNode[],
  options: RelaxProjectionOptions = {},
): ProjectedNode[] {
  const iterations = options.iterations ?? 4;
  const pushStrength = options.pushStrength ?? 28;
  const maxMove = options.maxMovePerIteration ?? 48;

  const next = nodes.map((node) => ({ ...node }));

  for (let iter = 0; iter < iterations; iter++) {
    for (let i = 0; i < next.length; i++) {
      for (let j = i + 1; j < next.length; j++) {
        const a = next[i];
        const b = next[j];

        if (!rectsOverlap(projectedNodeRect(a), projectedNodeRect(b), 12)) {
          continue;
        }

        /**
         * focus / selected 更稳定，普通节点让位。
         */
        const aFixed = a.isFocus || a.isSelected;
        const bFixed = b.isFocus || b.isSelected;

        const direction = overlapVector(projectedNodeRect(a), projectedNodeRect(b));

        const moveX = Math.max(-maxMove, Math.min(maxMove, direction.x * pushStrength));
        const moveY = Math.max(-maxMove, Math.min(maxMove, direction.y * pushStrength));

        if (aFixed && bFixed) {
          continue;
        }

        if (aFixed) {
          b.projectedX -= moveX;
          b.projectedY -= moveY;
          continue;
        }

        if (bFixed) {
          a.projectedX += moveX;
          a.projectedY += moveY;
          continue;
        }

        a.projectedX += moveX / 2;
        a.projectedY += moveY / 2;
        b.projectedX -= moveX / 2;
        b.projectedY -= moveY / 2;
      }
    }
  }

  return next;
}
```

---

## 8.1 semantic-projection.ts 接入 relaxation

修改：

```text
src/core/semantic-projection.ts
```

import：

```ts
import { relaxProjectedNodes } from "./layout-relaxation";
```

替换原来的 `resolveSimpleOverlap(projectedNodes)`：

```ts
projectedNodes = relaxProjectedNodes(projectedNodes, {
  iterations: doc.nodes.length > 300 ? 2 : 4,
  pushStrength: 28,
});
```

删除或保留旧 `resolveSimpleOverlap`，但不要再使用。

---

# 9. Preview Cache

当前 preview renderer 有局部缓存，但 Phase 4 单独抽出缓存层。

创建：

```text
src/core/preview-cache.ts
```

```ts
export interface PreviewCacheEntry {
  markdown: string;
  updatedAt: number;
}

export class PreviewCache {
  private cache = new Map<string, PreviewCacheEntry>();

  constructor(private maxEntries = 300) {}

  get(key: string): string | null {
    return this.cache.get(key)?.markdown ?? null;
  }

  set(key: string, markdown: string): void {
    this.cache.set(key, {
      markdown,
      updatedAt: Date.now(),
    });

    this.evictIfNeeded();
  }

  delete(key: string): void {
    this.cache.delete(key);
  }

  clear(): void {
    this.cache.clear();
  }

  private evictIfNeeded(): void {
    if (this.cache.size <= this.maxEntries) return;

    const entries = [...this.cache.entries()].sort(
      (a, b) => a[1].updatedAt - b[1].updatedAt,
    );

    while (entries.length > this.maxEntries) {
      const [key] = entries.shift()!;
      this.cache.delete(key);
    }
  }
}

export const globalPreviewCache = new PreviewCache();
```

---

## 9.1 notebook-preview-renderer.ts 接入

修改：

```text
src/renderer/notebook-preview-renderer.ts
```

删除本地：

```ts
const markdownCache = new Map<string, string>();
```

改为：

```ts
import { globalPreviewCache } from "../core/preview-cache";
```

读取时：

```ts
const cached = globalPreviewCache.get(cacheKey);
if (cached) return cached;
```

写入：

```ts
globalPreviewCache.set(cacheKey, result);
```

---

## 9.2 main.ts 监听文件修改清理缓存

在 `main.ts` 中：

```ts
import { globalPreviewCache } from "./core/preview-cache";
```

注册事件：

```ts
this.registerEvent(
  this.app.vault.on("modify", () => {
    globalPreviewCache.clear();
  }),
);
```

MVP 可以直接全清。后续再按 path 精确清理。

---

# 10. Canvas Background Renderer

大图时，低细节节点与边用 Canvas 渲染，SVG 只渲染交互层。

创建：

```text
src/renderer/canvas-background-renderer.ts
```

```ts
import type { ProjectedEdge, ProjectedNode } from "../types/mindmap";
import type { ViewTransform } from "../core/screen-transform";

export interface CanvasBackgroundRenderInput {
  canvas: HTMLCanvasElement;
  nodes: ProjectedNode[];
  edges: ProjectedEdge[];
  transform: ViewTransform;
}

export class CanvasBackgroundRenderer {
  render(input: CanvasBackgroundRenderInput): void {
    const { canvas, nodes, edges, transform } = input;

    const ctx = canvas.getContext("2d");
    if (!ctx) return;

    const rect = canvas.getBoundingClientRect();
    const dpr = window.devicePixelRatio || 1;

    canvas.width = rect.width * dpr;
    canvas.height = rect.height * dpr;

    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    ctx.clearRect(0, 0, rect.width, rect.height);

    ctx.save();
    ctx.translate(transform.x, transform.y);
    ctx.scale(transform.k, transform.k);

    this.drawEdges(ctx, nodes, edges);
    this.drawNodes(ctx, nodes);

    ctx.restore();
  }

  private drawNodes(ctx: CanvasRenderingContext2D, nodes: ProjectedNode[]): void {
    ctx.save();

    ctx.font = "12px sans-serif";
    ctx.textBaseline = "middle";

    for (const node of nodes) {
      /**
       * Canvas 背景层只画低细节节点。
       */
      ctx.beginPath();
      roundRect(ctx, node.projectedX, node.projectedY, node.displayWidth, node.displayHeight, 8);
      ctx.stroke();

      ctx.fillText(
        node.title,
        node.projectedX + 8,
        node.projectedY + Math.min(node.displayHeight / 2, 18),
      );
    }

    ctx.restore();
  }

  private drawEdges(
    ctx: CanvasRenderingContext2D,
    nodes: ProjectedNode[],
    edges: ProjectedEdge[],
  ): void {
    const nodeMap = new Map(nodes.map((node) => [node.id, node]));

    ctx.save();
    ctx.beginPath();

    for (const edge of edges) {
      const source = nodeMap.get(edge.source);
      const target = nodeMap.get(edge.target);

      if (!source || !target) continue;

      const sx = source.projectedX + source.displayWidth / 2;
      const sy = source.projectedY + source.displayHeight / 2;
      const tx = target.projectedX + target.displayWidth / 2;
      const ty = target.projectedY + target.displayHeight / 2;

      ctx.moveTo(sx, sy);
      ctx.lineTo(tx, ty);
    }

    ctx.stroke();
    ctx.restore();
  }
}

function roundRect(
  ctx: CanvasRenderingContext2D,
  x: number,
  y: number,
  width: number,
  height: number,
  radius: number,
): void {
  const r = Math.min(radius, width / 2, height / 2);

  ctx.moveTo(x + r, y);
  ctx.arcTo(x + width, y, x + width, y + height, r);
  ctx.arcTo(x + width, y + height, x, y + height, r);
  ctx.arcTo(x, y + height, x, y, r);
  ctx.arcTo(x, y, x + width, y, r);
  ctx.closePath();
}
```

---

# 11. Hybrid Renderer

创建：

```text
src/renderer/hybrid-mindmap-renderer.ts
```

## 11.1 策略

```text
Canvas：
- detailLevel <= 1 的普通背景节点
- 非 selected / non-hovered / non-focus 节点
- 大量低细节 edge

SVG：
- selected
- hovered
- focus
- ancestor path
- detailLevel >= 2
- notebook preview 节点
```

---

## 11.2 分区工具

创建：

```text
src/core/render-partition.ts
```

```ts
import type { ProjectedEdge, ProjectedNode } from "../types/mindmap";

export interface RenderPartition {
  canvasNodes: ProjectedNode[];
  canvasEdges: ProjectedEdge[];
  svgNodes: ProjectedNode[];
  svgEdges: ProjectedEdge[];
}

export function partitionForHybridRender(
  nodes: ProjectedNode[],
  edges: ProjectedEdge[],
): RenderPartition {
  const svgNodeIds = new Set<string>();
  const canvasNodeIds = new Set<string>();

  for (const node of nodes) {
    if (
      node.isSelected ||
      node.isHovered ||
      node.isFocus ||
      node.isAncestorPath ||
      node.detailLevel >= 2 ||
      node.kind === "notebook"
    ) {
      svgNodeIds.add(node.id);
    } else {
      canvasNodeIds.add(node.id);
    }
  }

  const svgNodes = nodes.filter((node) => svgNodeIds.has(node.id));
  const canvasNodes = nodes.filter((node) => canvasNodeIds.has(node.id));

  const svgEdges = edges.filter(
    (edge) => svgNodeIds.has(edge.source) || svgNodeIds.has(edge.target),
  );

  const canvasEdges = edges.filter(
    (edge) => canvasNodeIds.has(edge.source) && canvasNodeIds.has(edge.target),
  );

  return {
    svgNodes,
    svgEdges,
    canvasNodes,
    canvasEdges,
  };
}
```

---

## 11.3 Hybrid Renderer 骨架

实现时可以复用 `SvgMindmapRenderer` 的大部分逻辑，但不要继承，直接组合。

创建：

```text
src/renderer/hybrid-mindmap-renderer.ts
```

```ts
import * as d3 from "d3";
import { App } from "obsidian";
import type { MindmapDocument, NodeDetailLevel } from "../types/mindmap";
import type { RendererAdapter } from "./renderer-adapter";
import { createSemanticProjection } from "../core/semantic-projection";
import { partitionForHybridRender } from "../core/render-partition";
import { CanvasBackgroundRenderer } from "./canvas-background-renderer";
import { renderProjectedEdges } from "./projected-edge-renderer";
import { renderProjectedNodes } from "./projected-node-renderer";
import { InlineTitleEditor } from "./inline-title-editor";

export class HybridMindmapRenderer implements RendererAdapter {
  private root!: HTMLDivElement;
  private canvas!: HTMLCanvasElement;
  private svg!: d3.Selection<SVGSVGElement, unknown, null, undefined>;

  private svgEdgeLayer!: d3.Selection<SVGGElement, unknown, null, undefined>;
  private svgNodeLayer!: d3.Selection<SVGGElement, unknown, null, undefined>;

  private inlineEditorLayer!: HTMLDivElement;

  private canvasRenderer = new CanvasBackgroundRenderer();
  private zoomBehavior!: d3.ZoomBehavior<SVGSVGElement, unknown>;

  private hoveredNodeId: string | undefined;
  private lastFocusNodeId: string | undefined;
  private forcedDetailLevel = new Map<string, NodeDetailLevel>();
  private searchResultIds = new Set<string>();
  private connectionSourceId: string | undefined;

  constructor(
    private options: {
      app: App;
      container: HTMLElement;
      sourcePath: string;

      getDocument: () => MindmapDocument;
      getSelectedNodeIds: () => string[];

      onViewportChange: (x: number, y: number, zoom: number) => void;
      onSelectNode: (id: string, mode: "replace" | "toggle" | "add") => void;
      onToggleTree: (id: string) => void;
      notebook actions moved to context menu
      onInlineTitleCommit: (id: string, title: string) => Promise<void>;
      onContextMenu: (id: string, x: number, y: number) => void;
      onEdgeContextMenu: (id: string, x: number, y: number) => void;

      onBeforeNodeDragStart: () => void;
      onNodesMove: (moves: Array<{ id: string; x: number; y: number }>) => void;
      onNodeDragEnd: () => void;

      onBoxSelect: (rect: { x: number; y: number; width: number; height: number }) => void;

      onRenderStats?: (stats: {
        zoom: number;
        totalNodes: number;
        renderedNodes: number;
        totalEdges: number;
        renderedEdges: number;
      }) => void;
    },
  ) {}

  mount(): void {
    this.options.container.empty();

    this.root = this.options.container.createDiv({
      cls: "hybrid-renderer-root",
    });

    this.canvas = this.root.createEl("canvas", {
      cls: "hybrid-background-canvas",
    });

    this.svg = d3
      .select(this.root)
      .append("svg")
      .attr("class", "hybrid-interaction-svg")
      .attr("width", "100%")
      .attr("height", "100%");

    this.svgEdgeLayer = this.svg.append("g").attr("class", "svg-edge-layer");
    this.svgNodeLayer = this.svg.append("g").attr("class", "svg-node-layer");

    this.inlineEditorLayer = this.root.createDiv({
      cls: "inline-editor-layer",
    });

    this.zoomBehavior = d3
      .zoom<SVGSVGElement, unknown>()
      .scaleExtent([0.12, 4])
      .on("zoom", (event) => {
        const t = event.transform;
        this.options.onViewportChange(t.x, t.y, t.k);
        this.render();
      });

    this.svg.call(this.zoomBehavior);

    const viewport = this.options.getDocument().viewport;

    this.svg.call(
      this.zoomBehavior.transform,
      d3.zoomIdentity.translate(viewport.x, viewport.y).scale(viewport.zoom),
    );

    this.render();
  }

  unmount(): void {
    this.options.container.empty();
  }

  render(): void {
    const doc = this.options.getDocument();
    const transform = d3.zoomTransform(this.svg.node()!);

    const projection = createSemanticProjection(
      doc,
      {
        zoom: transform.k,
        viewportWorldRect: this.getViewportWorldRect(),
        selectedNodeIds: this.options.getSelectedNodeIds(),
        hoveredNodeId: this.hoveredNodeId,
        lastFocusNodeId: this.lastFocusNodeId,
      },
      {
        searchResultIds: this.searchResultIds,
        connectionSourceId: this.connectionSourceId,
      },
    );

    for (const node of projection.nodes) {
      const forced = this.forcedDetailLevel.get(node.id);
      if (forced !== undefined && forced > node.detailLevel) {
        node.detailLevel = forced;
      }
    }

    const partition = partitionForHybridRender(projection.nodes, projection.edges);

    this.canvasRenderer.render({
      canvas: this.canvas,
      nodes: partition.canvasNodes,
      edges: partition.canvasEdges,
      transform: {
        x: transform.x,
        y: transform.y,
        k: transform.k,
      },
    });

    this.svgEdgeLayer.attr("transform", transform.toString());

    renderProjectedEdges({
      edgeLayer: this.svgEdgeLayer,
      nodes: projection.nodes,
      edges: partition.svgEdges,
      onEdgeContextMenu: this.options.onEdgeContextMenu,
    });

    renderProjectedNodes({
      app: this.options.app,
      nodeLayer: this.svgNodeLayer,
      nodes: partition.svgNodes,
      transform: {
        x: transform.x,
        y: transform.y,
        k: transform.k,
      },
      sourcePath: this.options.sourcePath,

      getSelectedNodeIds: this.options.getSelectedNodeIds,

      onSelectNode: (id, mode) => {
        this.lastFocusNodeId = id;
        this.options.onSelectNode(id, mode);
        this.render();
      },

      onHoverNode: (id) => {
        this.hoveredNodeId = id;
        this.render();
      },

      onLeaveNode: () => {
        this.hoveredNodeId = undefined;
        this.render();
      },

      onToggleTree: this.options.onToggleTree,
      notebook actions moved to context menu

      onStartInlineEdit: (node, rect) => {
        new InlineTitleEditor({
          layer: this.inlineEditorLayer,
          x: rect.x,
          y: rect.y,
          width: rect.width,
          value: node.title,
          onCommit: async (value) => {
            await this.options.onInlineTitleCommit(node.id, value);
          },
          onCancel: () => {},
        }).open();
      },

      onContextMenu: this.options.onContextMenu,

      onBeforeNodeDragStart: this.options.onBeforeNodeDragStart,
      onNodesMove: this.options.onNodesMove,
      onNodeDragEnd: this.options.onNodeDragEnd,
    });

    this.options.onRenderStats?.({
      zoom: transform.k,
      totalNodes: projection.nodes.length,
      renderedNodes: partition.svgNodes.length + partition.canvasNodes.length,
      totalEdges: projection.edges.length,
      renderedEdges: partition.svgEdges.length + partition.canvasEdges.length,
    });
  }

  focusNode(id: string): void {
    const node = this.options.getDocument().nodes.find((item) => item.id === id);
    if (!node) return;

    const rect = this.options.container.getBoundingClientRect();
    const current = d3.zoomTransform(this.svg.node()!);
    const k = Math.max(current.k, 1.2);

    const x = rect.width / 2 - node.x * k;
    const y = rect.height / 2 - node.y * k;

    this.svg
      .transition()
      .duration(250)
      .call(this.zoomBehavior.transform, d3.zoomIdentity.translate(x, y).scale(k));
  }

  setLastFocusNodeId(id: string): void {
    this.lastFocusNodeId = id;
  }

  forceDetailLevel(id: string, level: number): void {
    this.forcedDetailLevel.set(id, level as NodeDetailLevel);
  }

  setSearchResultIds(ids: Set<string>): void {
    this.searchResultIds = new Set(ids);
  }

  setConnectionState(state: { enabled: boolean; sourceId?: string }): void {
    this.connectionSourceId = state.sourceId;
  }

  private getViewportWorldRect(): { x: number; y: number; width: number; height: number } {
    const transform = d3.zoomTransform(this.svg.node()!);
    const rect = this.options.container.getBoundingClientRect();

    const topLeft = transform.invert([0, 0]);
    const bottomRight = transform.invert([rect.width, rect.height]);

    return {
      x: topLeft[0],
      y: topLeft[1],
      width: bottomRight[0] - topLeft[0],
      height: bottomRight[1] - topLeft[1],
    };
  }
}
```

---

## 11.4 CSS

```css
.hybrid-renderer-root {
  position: relative;
  width: 100%;
  height: 100%;
}

.hybrid-background-canvas {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
}

.hybrid-interaction-svg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
}
```

---

# 12. MindmapView 自动选择 Renderer

修改：

```text
src/view/mindmap-view.ts
```

import：

```ts
import { chooseRenderMode } from "../core/render-mode";
import type { RendererAdapter } from "../renderer/renderer-adapter";
import { HybridMindmapRenderer } from "../renderer/hybrid-mindmap-renderer";
```

原成员：

```ts
private renderer: SvgMindmapRenderer | null = null;
```

改为：

```ts
private renderer: RendererAdapter | null = null;
```

创建 renderer 时：

```ts
const doc = this.store.getDocument();

const renderMode = chooseRenderMode({
  nodeCount: doc.nodes.length,
  edgeCount: doc.edges.length,
});

const RendererClass = renderMode === "hybrid"
  ? HybridMindmapRenderer
  : SvgMindmapRenderer;

this.renderer = new RendererClass({
  // same options
});
```

注意：

```text
1. Renderer 自动选择只在 renderView() 创建时执行。
2. 不要每次 render 动态切换。
3. 如果节点数量变化很大，用户重新打开文件后才切换即可。
```

---

# 13. SvgMindmapRenderer 也接入 PerformanceMonitor

在两个 renderer 中都使用 `PerformanceMonitor`。

简单做法：

在 `SvgMindmapRenderer` 内：

```ts
import { PerformanceMonitor } from "../core/performance-monitor";

private performanceMonitor = new PerformanceMonitor();
```

render 内包裹核心渲染：

```ts
const start = performance.now();

// existing render logic

const durationMs = performance.now() - start;

this.options.onRenderStats?.({
  zoom: transform.k,
  totalNodes: projection.nodes.length,
  renderedNodes: renderNodes.length,
  totalEdges: projection.edges.length,
  renderedEdges: renderEdges.length,
});
```

如果要使用完整 `PerformanceMonitor`，也可以，但 MVP 只要能显示 stats 即可。

---

# 14. 交互稳定性优化

## 14.1 拖拽时不要触发 hover 重渲染

在 renderer 里增加：

```ts
private dragging = false;
```

drag start：

```ts
this.dragging = true;
```

drag end：

```ts
this.dragging = false;
```

hover 时：

```ts
if (this.dragging) return;
```

需要把 drag 状态从 `projected-node-renderer` 回调到 renderer。

可在 options 增加：

```ts
onDragStateChange?: (dragging: boolean) => void;
```

在 drag start/end 调用。

---

## 14.2 zoom render 节流

滚轮缩放会频繁触发 render。增加 requestAnimationFrame 节流。

在 renderer 中：

```ts
private renderScheduled = false;

scheduleRender(): void {
  if (this.renderScheduled) return;

  this.renderScheduled = true;

  requestAnimationFrame(() => {
    this.renderScheduled = false;
    this.render();
  });
}
```

zoom handler 从：

```ts
this.render();
```

改为：

```ts
this.scheduleRender();
```

注意：

```text
用户编辑、拖拽结束、创建节点等仍可直接 render。
zoom/pan 用 scheduleRender。
```

---

## 14.3 forcedDetailLevel 自动清理

当前 notebook 点击可能一直 force L5。Phase 4 增加清理规则：

```text
1. 用户选择其他节点时，清理非选中节点 forced detail。
2. 缩放到低 zoom 时，可清理 forced detail。
```

在 renderer：

```ts
clearForcedDetailExcept(nodeId?: string): void {
  for (const key of [...this.forcedDetailLevel.keys()]) {
    if (key !== nodeId) {
      this.forcedDetailLevel.delete(key);
    }
  }
}
```

在 `onSelectNode`：

```ts
this.clearForcedDetailExcept(id);
```

---

# 15. Phase 4 验收标准

完成后必须满足：

```text
1. 没有任何 LLM 功能。
2. 没有 AI 摘要。
3. 没有 AI 自动生成脑图。
4. 没有 AI 自动布局。
5. semantic zoom 策略被抽到 semantic-zoom-policy.ts。
6. text 节点最高 detailLevel 视觉不超过 L2。
7. notebook 节点可以显示到 L5 preview。
8. 非焦点远端节点会根据距离降级。
9. 局部避让只修改 projectedX / projectedY。
10. 局部避让不写回 JSON。
11. preview cache 独立在 preview-cache.ts。
12. note 修改后 preview cache 会清理。
13. 1200+ 节点自动使用 hybrid renderer。
14. hybrid renderer 中 Canvas 负责低细节背景节点。
15. hybrid renderer 中 SVG 负责 focus / selected / hovered / notebook / detailLevel >= 2 节点。
16. SVG renderer 仍可用于小图。
17. renderer adapter 抽象存在。
18. render mode 只在打开视图时选择，不在每次 render 切换。
19. debug overlay 可以显示 render stats。
20. zoom render 使用 requestAnimationFrame 节流。
21. 拖拽时 hover 不导致频繁重渲染。
22. forced detail 会在切换节点时清理。
23. tile index 不参与语义聚合。
24. reference edge 不参与 radial layout。
25. text 节点仍然只是一句话。
26. notebook 文件不会被自动删除。
```

---

# 16. Phase 4 禁止事项

执行模型必须遵守：

```text
1. 禁止接入任何 LLM API。
2. 禁止调用任何 AI 服务。
3. 禁止生成 AI 摘要。
4. 禁止生成 AI 推荐节点。
5. 禁止 AI 自动整理布局。
6. 禁止 AI 自动生成 notebook 内容。
7. 禁止空间 cluster 作为语义聚合。
8. 禁止 tile index 影响 semantic projection。
9. 禁止让 Canvas 承担 notebook preview。
10. 禁止让 text 节点显示 Markdown preview。
11. 禁止把 projected 坐标写回 JSON。
12. 禁止让 reference edge 参与主树布局。
13. 禁止每次 render 动态切换 renderer。
14. 禁止恢复右侧圆点连接控件。
```

---

# 17. 推荐实施顺序

严格按下面顺序执行：

```text
1. 创建 renderer-adapter.ts
2. 让 SvgMindmapRenderer 实现 RendererAdapter
3. 创建 render-mode.ts
4. MindmapView 改为 RendererAdapter 类型
5. 创建 performance-monitor.ts
6. 创建 performance-debug-overlay.ts
7. renderer 增加 onRenderStats
8. 创建 semantic-zoom-policy.ts
9. semantic-projection.ts 接入 semantic-zoom-policy
10. 创建 layout-collision.ts
11. 创建 layout-relaxation.ts
12. semantic-projection.ts 接入 relaxProjectedNodes
13. 创建 preview-cache.ts
14. notebook-preview-renderer.ts 接入 preview-cache
15. main.ts 监听 modify 清理 preview cache
16. 创建 canvas-background-renderer.ts
17. 创建 render-partition.ts
18. 创建 hybrid-mindmap-renderer.ts
19. MindmapView 接入 chooseRenderMode
20. CSS 添加 hybrid renderer 样式
21. renderer 增加 requestAnimationFrame render 节流
22. renderer 优化 dragging hover 行为
23. renderer 清理 forced detail
24. 全量回归测试
```

---

# 18. Phase 4 完成后的状态

完成 Phase 4 后，插件应该具备：

```text
1. 小图使用 SVG renderer
2. 大图使用 Canvas/SVG hybrid renderer
3. notebook preview 有缓存
4. 渲染性能可观察
5. semantic zoom 策略更稳定
6. 局部节点避让更自然
7. 大图拖拽和缩放更流畅
8. 仍然完全不依赖大模型
```

Phase 5 再考虑：

```text
1. 更复杂的布局算法
2. 更精细的 edge routing
3. 更完整的 keyboard navigation
4. 更强的 notebook heading/block 预览
5. 移动端/平板交互
6. 导出 SVG/PNG
7. 可选的插件生态集成
```
