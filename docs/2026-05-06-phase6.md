Phase 6 目标是把插件推进到 **可发布、可维护、可升级** 的状态。

---

# Phase 6：发布准备、工程质量与插件商店化执行计划

## 0. 当前项目状态假设

已经完成：

```text
Phase 1：
- text / notebook 节点
- inline edit
- notebook 自动创建 / 绑定 / 预览
- semantic zoom
- radial layout

Phase 2：
- 拖拽
- Tab / Enter 新增节点
- 搜索
- 多选 / 框选
- reference 连线
- undo / redo

Phase 3：
- 设置页
- Markdown headings → mindmap
- backlinks / outlinks → local map
- viewport culling
- debug overlay

Phase 4：
- SVG / Hybrid renderer
- Canvas 背景层
- performance monitor
- preview cache
- semantic zoom policy
- layout relaxation

Phase 5：
- keyboard navigation
- edge routing
- heading / block preview
- SVG / PNG 导出
- minimap
- missing notebook 修复
- dirty state
- sample data
```

---

# 1. Phase 6 功能范围

## 1.1 本阶段实现

```text
1. 完整插件设置页
2. 数据版本迁移系统
3. manifest / versions / release 配置
4. 命令体系整理
5. 快捷键说明与帮助面板
6. 错误处理与通知系统
7. 自动化测试基础
8. 样例 vault / demo 文件生成
9. 发布文档 README
10. 用户迁移说明
11. 插件商店发布前检查
12. 性能回归检查
13. 可访问性基础优化
14. 国际化预留
```

---

## 1.2 本阶段不实现

```text
1. 不实现任何 LLM 功能
2. 不做 AI 内容生成
3. 不做 AI 摘要
4. 不做 AI 推荐
5. 不做多人协作
6. 不做云同步
7. 不做账号系统
8. 不做远程服务器
9. 不做完整移动端适配
```

---

# 2. 新增目录结构

新增：

```text
src/
  core/
    versioning.ts
    command-registry.ts
    error-boundary.ts
    telemetry-disabled.ts
    accessibility.ts
    i18n.ts

  migrations/
    migrate-v1-to-v2.ts
    migration-runner.ts

  ui/
    help-modal.ts
    about-modal.ts
    error-notice.ts

  test/
    test-fixtures.ts
    document-store.test.ts
    hierarchy.test.ts
    semantic-projection.test.ts
    radial-layout.test.ts
    notebook-service.test.ts
```

项目根目录新增：

```text
manifest.json
versions.json
README.md
CHANGELOG.md
LICENSE
esbuild.config.mjs
package.json
tsconfig.json
```

---

# 3. 完整设置页增强

Phase 3 设置页已有基础选项。Phase 6 把设置系统补全。

## 3.1 扩展 settings.ts

修改：

```text
src/types/settings.ts
```

```ts
export interface SemanticMindmapSettings {
  notebookFolder: string;
  notebookTemplate: string;

  importHeadingsAsNotebookNodes: boolean;
  maxBacklinkMapNodes: number;

  showDebugOverlay: boolean;
  showMinimap: boolean;

  defaultRenderMode: "auto" | "svg" | "hybrid";

  /**
   * 是否启用 Canvas 背景层。
   */
  enableHybridRenderer: boolean;

  /**
   * 大于多少节点时切换 hybrid。
   */
  hybridNodeThreshold: number;

  /**
   * 是否启用 viewport culling。
   */
  enableViewportCulling: boolean;

  /**
   * 大于多少节点时启用 culling。
   */
  cullingNodeThreshold: number;

  /**
   * 是否显示 missing notebook 标记。
   */
  showMissingNotebookWarnings: boolean;

  /**
   * 是否自动保存。
   */
  autoSave: boolean;

  /**
   * 自动保存延迟。
   */
  autoSaveDelayMs: number;

  /**
   * 导出默认格式。
   */
  defaultExportFormat: "svg" | "png";

  /**
   * UI 语言预留。
   */
  language: "auto" | "zh" | "en";
}

export const DEFAULT_SETTINGS: SemanticMindmapSettings = {
  notebookFolder: "notebooks",
  notebookTemplate: "# {{title}}\n",

  importHeadingsAsNotebookNodes: true,
  maxBacklinkMapNodes: 80,

  showDebugOverlay: false,
  showMinimap: true,

  defaultRenderMode: "auto",

  enableHybridRenderer: true,
  hybridNodeThreshold: 1200,

  enableViewportCulling: true,
  cullingNodeThreshold: 500,

  showMissingNotebookWarnings: true,

  autoSave: true,
  autoSaveDelayMs: 800,

  defaultExportFormat: "svg",

  language: "auto",
};
```

---

## 3.2 设置页分组

修改：

```text
src/ui/settings-tab.ts
```

建议设置页分组：

```text
1. Notebook
2. Import
3. Rendering
4. Performance
5. Export
6. Debug
7. Advanced
```

核心代码结构：

```ts
display(): void {
  const { containerEl } = this;
  containerEl.empty();

  containerEl.createEl("h2", { text: "Semantic Zoom Mindmap 设置" });

  this.renderNotebookSettings(containerEl);
  this.renderImportSettings(containerEl);
  this.renderRenderingSettings(containerEl);
  this.renderPerformanceSettings(containerEl);
  this.renderExportSettings(containerEl);
  this.renderDebugSettings(containerEl);
  this.renderAdvancedSettings(containerEl);
}
```

新增分组 helper：

```ts
private createSection(containerEl: HTMLElement, title: string, desc?: string): void {
  containerEl.createEl("h3", { text: title });

  if (desc) {
    containerEl.createEl("p", {
      text: desc,
      cls: "setting-item-description",
    });
  }
}
```

---

## 3.3 Rendering settings 示例

```ts
private renderRenderingSettings(containerEl: HTMLElement): void {
  this.createSection(containerEl, "渲染");

  new Setting(containerEl)
    .setName("显示小地图")
    .setDesc("在右上角显示 minimap。")
    .addToggle((toggle) =>
      toggle
        .setValue(this.plugin.settings.showMinimap)
        .onChange(async (value) => {
          this.plugin.settings.showMinimap = value;
          await this.plugin.saveSettings();
        }),
    );

  new Setting(containerEl)
    .setName("默认渲染模式")
    .setDesc("auto 会根据节点数量选择 SVG 或 Hybrid。")
    .addDropdown((dropdown) =>
      dropdown
        .addOption("auto", "Auto")
        .addOption("svg", "SVG")
        .addOption("hybrid", "Hybrid")
        .setValue(this.plugin.settings.defaultRenderMode)
        .onChange(async (value) => {
          this.plugin.settings.defaultRenderMode = value as "auto" | "svg" | "hybrid";
          await this.plugin.saveSettings();
        }),
    );
}
```

---

## 3.4 Performance settings 示例

```ts
private renderPerformanceSettings(containerEl: HTMLElement): void {
  this.createSection(containerEl, "性能");

  new Setting(containerEl)
    .setName("启用 Hybrid Renderer")
    .setDesc("大图时使用 Canvas 背景层 + SVG 交互层。")
    .addToggle((toggle) =>
      toggle
        .setValue(this.plugin.settings.enableHybridRenderer)
        .onChange(async (value) => {
          this.plugin.settings.enableHybridRenderer = value;
          await this.plugin.saveSettings();
        }),
    );

  new Setting(containerEl)
    .setName("Hybrid 节点阈值")
    .setDesc("节点数超过该值时，auto 模式使用 Hybrid renderer。")
    .addText((text) =>
      text
        .setValue(String(this.plugin.settings.hybridNodeThreshold))
        .onChange(async (value) => {
          const n = Number.parseInt(value, 10);
          this.plugin.settings.hybridNodeThreshold = Number.isFinite(n)
            ? Math.max(100, n)
            : 1200;
          await this.plugin.saveSettings();
        }),
    );

  new Setting(containerEl)
    .setName("启用 viewport culling")
    .setDesc("大图时只渲染视口附近的节点。")
    .addToggle((toggle) =>
      toggle
        .setValue(this.plugin.settings.enableViewportCulling)
        .onChange(async (value) => {
          this.plugin.settings.enableViewportCulling = value;
          await this.plugin.saveSettings();
        }),
    );
}
```

---

# 4. Render Mode 使用设置

修改：

```text
src/core/render-mode.ts
```

```ts
import type { SemanticMindmapSettings } from "../types/settings";

export type RenderMode = "svg" | "hybrid";

export function chooseRenderMode(args: {
  nodeCount: number;
  edgeCount: number;
  settings: SemanticMindmapSettings;
}): RenderMode {
  if (args.settings.defaultRenderMode === "svg") return "svg";

  if (args.settings.defaultRenderMode === "hybrid") {
    return args.settings.enableHybridRenderer ? "hybrid" : "svg";
  }

  if (!args.settings.enableHybridRenderer) return "svg";

  if (args.nodeCount >= args.settings.hybridNodeThreshold) {
    return "hybrid";
  }

  return "svg";
}
```

修改 `MindmapView` 中：

```ts
const renderMode = chooseRenderMode({
  nodeCount: doc.nodes.length,
  edgeCount: doc.edges.length,
  settings: this.plugin.settings,
});
```

---

# 5. 数据版本迁移系统

当前 `MindmapDocument.version = 1`。Phase 6 建立可扩展迁移机制。

## 5.1 versioning.ts

创建：

```text
src/core/versioning.ts
```

```ts
export const CURRENT_DOCUMENT_VERSION = 1;

export function isSupportedDocumentVersion(version: unknown): boolean {
  return typeof version === "number" && version >= 1 && version <= CURRENT_DOCUMENT_VERSION;
}
```

---

## 5.2 migration-runner.ts

创建：

```text
src/migrations/migration-runner.ts
```

```ts
import type { MindmapDocument } from "../types/mindmap";
import { CURRENT_DOCUMENT_VERSION } from "../core/versioning";

export type Migration = (doc: any) => any;

const migrations: Record<number, Migration> = {
  /**
   * version 1 是当前初始版本。
   * 未来新增：
   * 1: migrateV1ToV2
   */
};

export function runMigrations(input: any): MindmapDocument {
  let doc = input;

  let version = typeof doc.version === "number" ? doc.version : 1;

  while (version < CURRENT_DOCUMENT_VERSION) {
    const migration = migrations[version];

    if (!migration) {
      throw new Error(`Missing migration for document version ${version}`);
    }

    doc = migration(doc);
    version += 1;
    doc.version = version;
  }

  return doc as MindmapDocument;
}
```

---

## 5.3 document-migration.ts 接入 runner

修改：

```text
src/core/document-migration.ts
```

在 normalize 前：

```ts
import { runMigrations } from "../migrations/migration-runner";
```

```ts
export function migrateDocument(input: Partial<MindmapDocument>): MindmapDocument {
  const migrated = runMigrations(input);

  const doc = migrated as MindmapDocument;

  return {
    version: 1,
    title: doc.title ?? "Untitled Mindmap",
    layoutMode: doc.layoutMode ?? "radial",
    viewport: doc.viewport ?? { x: 0, y: 0, zoom: 1 },
    nodes: Array.isArray(doc.nodes) ? doc.nodes.map(migrateNode) : [],
    edges: Array.isArray(doc.edges)
      ? doc.edges.map((edge) => ({
          ...edge,
          relation: edge.relation ?? "mindmap",
          type: edge.type ?? "curve",
        }))
      : [],
  };
}
```

---

# 6. Command Registry 整理

当前命令可能分散在 `main.ts`。Phase 6 抽出命令注册。

## 6.1 command-registry.ts

创建：

```text
src/core/command-registry.ts
```

```ts
import { TFile } from "obsidian";
import type SemanticZoomMindmapPlugin from "../main";

export function registerMindmapCommands(plugin: SemanticZoomMindmapPlugin): void {
  plugin.addCommand({
    id: "create-semantic-zoom-mindmap",
    name: "Create semantic zoom mindmap",
    callback: async () => {
      const file = await plugin.createMindmapFile();
      await plugin.openMindmapFile(file);
    },
  });

  plugin.addCommand({
    id: "open-current-mindmap-json",
    name: "Open current .mindmap.json",
    checkCallback: (checking) => {
      const file = plugin.app.workspace.getActiveFile();
      const canRun = Boolean(file && file.path.endsWith(".mindmap.json"));

      if (checking) return canRun;

      if (file) {
        void plugin.openMindmapFile(file);
      }

      return true;
    },
  });

  plugin.addCommand({
    id: "create-mindmap-from-current-markdown-headings",
    name: "Create mindmap from current markdown headings",
    checkCallback: (checking) => {
      const file = plugin.app.workspace.getActiveFile();
      const canRun = Boolean(file && file.extension === "md");

      if (checking) return canRun;

      if (file instanceof TFile) {
        void plugin.createMindmapFromMarkdownFile(file);
      }

      return true;
    },
  });

  plugin.addCommand({
    id: "create-local-knowledge-map-from-current-file",
    name: "Create local knowledge map from current file",
    checkCallback: (checking) => {
      const file = plugin.app.workspace.getActiveFile();
      const canRun = Boolean(file && file.extension === "md");

      if (checking) return canRun;

      if (file instanceof TFile) {
        void plugin.createLocalKnowledgeMapFromFile(file);
      }

      return true;
    },
  });

  plugin.addCommand({
    id: "create-sample-mindmap-1000",
    name: "Create sample mindmap with 1000 nodes",
    callback: async () => {
      await plugin.createSampleMindmapFile(1000);
    },
  });

  plugin.addCommand({
    id: "show-semantic-mindmap-help",
    name: "Show semantic mindmap help",
    callback: () => {
      plugin.showHelp();
    },
  });
}
```

---

## 6.2 main.ts 暴露 public 方法

将这些原 private 方法改成 public：

```ts
async createMindmapFile(): Promise<TFile> {}
async openMindmapFile(file: TFile): Promise<void> {}
async createMindmapFromMarkdownFile(file: TFile): Promise<void> {}
async createLocalKnowledgeMapFromFile(file: TFile): Promise<void> {}
async createSampleMindmapFile(nodeCount: number): Promise<void> {}
showHelp(): void {}
```

在 `onload()` 中：

```ts
import { registerMindmapCommands } from "./core/command-registry";

registerMindmapCommands(this);
```

---

# 7. Help Modal

## 7.1 目标

提供内置帮助：

```text
1. 节点类型说明
2. 双下箭头说明
3. + / - 说明
4. 快捷键说明
5. notebook 节点说明
6. 导入 / 导出说明
```

---

## 7.2 help-modal.ts

创建：

```text
src/ui/help-modal.ts
```

```ts
import { App, Modal } from "obsidian";

export class MindmapHelpModal extends Modal {
  constructor(app: App) {
    super(app);
  }

  onOpen(): void {
    const { contentEl } = this;
    contentEl.empty();

    contentEl.createEl("h2", { text: "Semantic Zoom Mindmap 帮助" });

    contentEl.createEl("h3", { text: "节点类型" });
    contentEl.createEl("p", {
      text: "text 节点只是一句话，用于脑图结构表达。notebook 节点绑定 Obsidian 笔记，用于知识展开。",
    });

    contentEl.createEl("h3", { text: "主要操作" });

    const list = contentEl.createEl("ul");

    [
      "双击节点标题：编辑标题",
      "点击双下箭头：text 节点转 notebook，notebook 节点展开预览",
      "点击右侧 + / -：展开或收起子树",
      "Tab：新增子节点",
      "Enter：新增兄弟节点",
      "Space：展开 / 收起当前节点",
      "F2：编辑当前节点",
      "方向键：在节点之间移动",
      "Cmd/Ctrl + Z：撤销",
      "Cmd/Ctrl + Shift + Z：重做",
      "Cmd/Ctrl + F：搜索",
      "Cmd/Ctrl + 0：回到 root",
    ].forEach((text) => {
      list.createEl("li", { text });
    });

    contentEl.createEl("h3", { text: "语义缩放" });
    contentEl.createEl("p", {
      text: "缩放不会简单放大文字，而是改变信息粒度。缩小看结构，放大看 notebook 内容。",
    });
  }

  onClose(): void {
    this.contentEl.empty();
  }
}
```

---

## 7.3 main.ts 接入

```ts
import { MindmapHelpModal } from "./ui/help-modal";
```

```ts
showHelp(): void {
  new MindmapHelpModal(this.app).open();
}
```

Ribbon 增加帮助入口可选：

```ts
this.addRibbonIcon("help-circle", "Semantic Mindmap 帮助", () => {
  this.showHelp();
});
```

---

# 8. Error Notice 与错误边界

## 8.1 error-notice.ts

创建：

```text
src/ui/error-notice.ts
```

```ts
import { Notice } from "obsidian";

export function showErrorNotice(error: unknown, fallback = "操作失败"): void {
  const message = error instanceof Error ? error.message : fallback;
  new Notice(message, 6000);
}
```

---

## 8.2 error-boundary.ts

创建：

```text
src/core/error-boundary.ts
```

```ts
export async function safeAsync(
  fn: () => Promise<void>,
  onError: (error: unknown) => void,
): Promise<void> {
  try {
    await fn();
  } catch (error) {
    onError(error);
  }
}

export function safeSync(
  fn: () => void,
  onError: (error: unknown) => void,
): void {
  try {
    fn();
  } catch (error) {
    onError(error);
  }
}
```

---

## 8.3 MindmapView 中替换裸 try/catch

例如：

```ts
import { showErrorNotice } from "../ui/error-notice";
```

将：

```ts
try {
  ...
} catch (error) {
  new Notice(error instanceof Error ? error.message : "无法创建 notebook");
}
```

替换为：

```ts
try {
  ...
} catch (error) {
  showErrorNotice(error, "无法创建 notebook");
}
```

---

# 9. Accessibility 基础

## 9.1 accessibility.ts

创建：

```text
src/core/accessibility.ts
```

```ts
export function setButtonA11y(
  el: HTMLElement,
  label: string,
  pressed?: boolean,
): void {
  el.setAttr("aria-label", label);

  if (pressed !== undefined) {
    el.setAttr("aria-pressed", pressed ? "true" : "false");
  }
}

export function setCanvasA11y(el: HTMLElement): void {
  el.setAttr("role", "application");
  el.setAttr("aria-label", "Semantic Zoom Mindmap canvas");
}
```

---

## 9.2 MindmapView 接入

Canvas 创建后：

```ts
import { setCanvasA11y, setButtonA11y } from "../core/accessibility";
```

```ts
setCanvasA11y(canvas);
```

工具栏按钮设置：

```ts
setButtonA11y(addButton, "新增节点");
setButtonA11y(layoutButton, "中心布局");
setButtonA11y(saveButton, "保存脑图");
```

---

# 10. 国际化预留

不做完整 i18n，只抽象基础字典。

## 10.1 i18n.ts

创建：

```text
src/core/i18n.ts
```

```ts
export type Language = "zh" | "en";

const zh = {
  helpTitle: "Semantic Zoom Mindmap 帮助",
  createMindmap: "创建语义缩放脑图",
  save: "保存",
  exportSvg: "导出 SVG",
  exportPng: "导出 PNG",
};

const en = {
  helpTitle: "Semantic Zoom Mindmap Help",
  createMindmap: "Create semantic zoom mindmap",
  save: "Save",
  exportSvg: "Export SVG",
  exportPng: "Export PNG",
};

export function getLanguage(setting: "auto" | "zh" | "en"): Language {
  if (setting === "zh" || setting === "en") return setting;

  const locale = window.localStorage.getItem("language") ?? navigator.language;

  return locale.toLowerCase().startsWith("zh") ? "zh" : "en";
}

export function createTranslator(language: Language): (key: keyof typeof zh) => string {
  const dict = language === "zh" ? zh : en;

  return (key) => dict[key] ?? key;
}
```

Phase 6 不需要全量替换所有文案，只预留机制。

---

# 11. Telemetry Disabled 明确声明

为了用户信任，创建一个明确文件，说明没有遥测。

## 11.1 telemetry-disabled.ts

创建：

```text
src/core/telemetry-disabled.ts
```

```ts
export const TELEMETRY_ENABLED = false;

export function assertNoTelemetry(): void {
  /**
   * This plugin does not send analytics, telemetry, or user content to remote servers.
   */
}
```

在 `main.ts onload()` 调用：

```ts
import { assertNoTelemetry } from "./core/telemetry-disabled";

assertNoTelemetry();
```

---

# 12. 测试基础

## 12.1 package.json scripts

```json
{
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
```

如果项目暂时不使用 Vitest，可先建立测试文件，后续接入。

---

## 12.2 test fixtures

创建：

```text
src/test/test-fixtures.ts
```

```ts
import type { MindmapDocument } from "../types/mindmap";

export function createSmallTestDocument(): MindmapDocument {
  return {
    version: 1,
    title: "Test",
    layoutMode: "radial",
    viewport: { x: 0, y: 0, zoom: 1 },
    nodes: [
      {
        id: "root",
        kind: "text",
        title: "Root",
        x: 0,
        y: 0,
        width: 180,
        height: 56,
        treeControl: "manual-expanded",
      },
      {
        id: "child",
        kind: "text",
        title: "Child",
        x: 200,
        y: 0,
        width: 180,
        height: 56,
        treeControl: "auto",
      },
    ],
    edges: [
      {
        id: "edge1",
        source: "root",
        target: "child",
        relation: "mindmap",
        type: "curve",
      },
    ],
  };
}
```

---

## 12.3 hierarchy test

创建：

```text
src/test/hierarchy.test.ts
```

```ts
import { describe, expect, it } from "vitest";
import { buildHierarchy } from "../core/hierarchy";
import { createSmallTestDocument } from "./test-fixtures";

describe("buildHierarchy", () => {
  it("builds root and parent relation", () => {
    const doc = createSmallTestDocument();
    const hierarchy = buildHierarchy(doc);

    expect(hierarchy.rootId).toBe("root");
    expect(hierarchy.parentById.get("child")).toBe("root");
    expect(hierarchy.childrenById.get("root")).toContain("child");
  });
});
```

---

## 12.4 semantic projection test

创建：

```text
src/test/semantic-projection.test.ts
```

```ts
import { describe, expect, it } from "vitest";
import { createSemanticProjection } from "../core/semantic-projection";
import { createSmallTestDocument } from "./test-fixtures";

describe("createSemanticProjection", () => {
  it("keeps root visible", () => {
    const doc = createSmallTestDocument();

    const projection = createSemanticProjection(doc, {
      zoom: 0.2,
      viewportWorldRect: { x: -1000, y: -1000, width: 2000, height: 2000 },
      selectedNodeIds: [],
    });

    expect(projection.visibleNodeIds.has("root")).toBe(true);
  });

  it("shows edges when both endpoints are visible", () => {
    const doc = createSmallTestDocument();

    const projection = createSemanticProjection(doc, {
      zoom: 1,
      viewportWorldRect: { x: -1000, y: -1000, width: 2000, height: 2000 },
      selectedNodeIds: [],
    });

    expect(projection.edges.some((edge) => edge.id === "edge1")).toBe(true);
  });
});
```

---

## 12.5 radial layout test

创建：

```text
src/test/radial-layout.test.ts
```

```ts
import { describe, expect, it } from "vitest";
import { RadialLayoutEngine } from "../core/radial-layout";
import { createSmallTestDocument } from "./test-fixtures";

describe("RadialLayoutEngine", () => {
  it("puts root at center", () => {
    const doc = createSmallTestDocument();
    const next = new RadialLayoutEngine().layout(doc, "root");

    const root = next.nodes.find((node) => node.id === "root");

    expect(root?.x).toBe(0);
    expect(root?.y).toBe(0);
  });
});
```

---

# 13. Manifest / Versions / Build

## 13.1 manifest.json

项目根目录：

```json
{
  "id": "semantic-zoom-mindmap",
  "name": "Semantic Zoom Mindmap",
  "version": "0.1.0",
  "minAppVersion": "1.5.0",
  "description": "A semantic zoom mindmap plugin for Obsidian with text nodes, notebook nodes, and focus-preserving hierarchical zoom.",
  "author": "Your Name",
  "authorUrl": "",
  "isDesktopOnly": false
}
```

如果移动端暂不稳定，可以改：

```json
"isDesktopOnly": true
```

建议 MVP 发布前如果没有移动端测试，先设为：

```json
"isDesktopOnly": true
```

---

## 13.2 versions.json

```json
{
  "0.1.0": "1.5.0"
}
```

---

## 13.3 package.json

```json
{
  "name": "obsidian-semantic-zoom-mindmap",
  "version": "0.1.0",
  "description": "Semantic zoom mindmap plugin for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run"
  },
  "keywords": [
    "obsidian",
    "mindmap",
    "semantic-zoom",
    "knowledge-map"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.24.0",
    "obsidian": "latest",
    "tslib": "^2.6.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "d3": "^7.9.0"
  }
}
```

---

# 14. README

创建：

```text
README.md
```

结构：

````md
# Semantic Zoom Mindmap

Semantic Zoom Mindmap is an Obsidian plugin for building semantic zoom knowledge maps.

## Features

- Text nodes for one-sentence ideas
- Notebook nodes linked to Obsidian notes
- Focus-preserving semantic zoom
- Radial mindmap layout
- Inline editing
- Notebook preview
- Backlinks / outlinks local map
- Markdown headings import
- SVG / PNG export
- Minimap
- Keyboard navigation

## Node Types

### Text node

A text node is a simple one-sentence mindmap node.

### Notebook node

A notebook node links to an Obsidian note. It can preview note content when zoomed in.

## Basic Usage

- Double click node title to edit
- Right click a text node to create or bind a notebook
- Click + / - to expand or collapse subtree
- Tab to create child node
- Enter to create sibling node
- Space to expand/collapse selected node
- F2 to edit selected node
- Cmd/Ctrl + Z to undo
- Cmd/Ctrl + F to search

## File Format

Mindmaps are saved as `.mindmap.json`.

## Privacy

This plugin does not send analytics, telemetry, or user content to remote servers.

## Development

```bash
npm install
npm run dev
````

## Build

```bash
npm run build
```

````

---

# 15. CHANGELOG

创建：

```text
CHANGELOG.md
````

```md
# Changelog

## 0.1.0

Initial release.

- Text nodes
- Notebook nodes
- Semantic zoom
- Radial layout
- Inline editing
- Notebook preview
- Search
- Multi-select
- Reference edges
- SVG / PNG export
- Minimap
```

---

# 16. 发布前检查脚本

## 16.1 手动检查清单

创建：

```text
RELEASE_CHECKLIST.md
```

内容：

```md
# Release Checklist

## Build

- [ ] npm install
- [ ] npm run build
- [ ] npm test

## Files

- [ ] manifest.json exists
- [ ] versions.json exists
- [ ] main.js generated
- [ ] styles.css exists
- [ ] README.md exists
- [ ] LICENSE exists

## Obsidian Manual Test

- [ ] Plugin loads
- [ ] Create mindmap
- [ ] Open .mindmap.json
- [ ] Create text node
- [ ] Inline edit
- [ ] Convert text node to notebook node
- [ ] Preview notebook
- [ ] Tab creates child
- [ ] Enter creates sibling
- [ ] Search works
- [ ] Undo / redo works
- [ ] Export SVG works
- [ ] Export PNG works
- [ ] Minimap works
- [ ] Settings work
- [ ] No telemetry
```

---

# 17. Phase 6 验收标准

完成后必须满足：

```text
1. 设置页包含 Notebook / Import / Rendering / Performance / Export / Debug / Advanced 分组。
2. 设置项可以保存并在重启后恢复。
3. renderer 选择使用 settings。
4. notebook folder 使用 settings。
5. notebook template 使用 settings。
6. migration runner 存在。
7. document migration 使用 migration runner。
8. 命令注册从 main.ts 抽到 command-registry.ts。
9. Help modal 可打开。
10. Help modal 包含节点类型、快捷键、语义缩放说明。
11. 主要错误使用 showErrorNotice。
12. canvas 有 role 和 aria-label。
13. 主要按钮有 aria-label。
14. i18n 基础文件存在。
15. telemetry-disabled 文件存在，并明确无遥测。
16. 至少有 hierarchy / semantic projection / radial layout 测试。
17. manifest.json 存在。
18. versions.json 存在。
19. README.md 存在。
20. CHANGELOG.md 存在。
21. RELEASE_CHECKLIST.md 存在。
22. npm run build 可执行。
23. npm test 可执行。
24. 插件仍然不包含任何 LLM 功能。
25. 插件不发送远程请求。
```

---

# 18. Phase 6 禁止事项

执行模型必须遵守：

```text
1. 禁止接入任何 LLM。
2. 禁止调用远程 AI API。
3. 禁止加入遥测。
4. 禁止上传用户笔记内容。
5. 禁止引入账号系统。
6. 禁止为了测试修改核心数据模型。
7. 禁止破坏 .mindmap.json 兼容性。
8. 禁止把设置写入 mindmap 文件。
9. 禁止让 reference edge 参与主树布局。
10. 禁止让 text 节点支持 Markdown。
11. 禁止恢复右侧圆点连接控件。
12. 禁止删除 notebook 文件。
```

---

# 19. 推荐实施顺序

严格按下面顺序执行：

```text
1. 扩展 settings.ts
2. 设置页分组重构
3. render-mode.ts 接入 settings
4. MindmapView 使用 settings 选择 renderer
5. versioning.ts
6. migration-runner.ts
7. document-migration.ts 接入 migration-runner
8. command-registry.ts
9. main.ts 简化命令注册
10. help-modal.ts
11. main.ts 接入 showHelp
12. error-notice.ts
13. error-boundary.ts
14. 替换主要 Notice 错误处理
15. accessibility.ts
16. MindmapView 接入基础 a11y
17. i18n.ts
18. telemetry-disabled.ts
19. test fixtures
20. hierarchy test
21. semantic projection test
22. radial layout test
23. manifest.json
24. versions.json
25. package.json scripts
26. README.md
27. CHANGELOG.md
28. RELEASE_CHECKLIST.md
29. npm run build
30. npm test
31. Obsidian 手动回归测试
```

---

# 20. Phase 6 完成后的状态

完成 Phase 6 后，插件应达到：

```text
1. 功能闭环
2. 设置完整
3. 文档完整
4. 可构建
5. 可测试
6. 可发布
7. 可升级
8. 无遥测
9. 无大模型依赖
10. 可准备提交 Obsidian 插件社区审核
```
