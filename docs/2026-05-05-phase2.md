下面是 **Phase 2 详细执行计划**，可以直接复制给编码模型执行。

Phase 2 目标：

```text
1. Tab 新增子节点
2. Enter 新增兄弟节点
3. 节点拖拽
4. 连线编辑
5. undo / redo
6. 搜索节点
7. 多选 / 框选
8. 基础键盘操作
```

---

# Phase 2：编辑能力增强执行计划

## 0. 当前 Phase 1 已有基础

假设 Phase 1 已经完成：

```text
src/
  main.ts
  constants.ts

  types/
    mindmap.ts

  core/
    document-store.ts
    hierarchy.ts
    focus.ts
    detail-level.ts
    semantic-projection.ts
    radial-layout.ts
    selection.ts
    notebook-service.ts
    autosave.ts
    id.ts
    screen-transform.ts

  view/
    mindmap-view.ts

  renderer/
    svg-mindmap-renderer.ts
    projected-node-renderer.ts
    projected-edge-renderer.ts
    inline-title-editor.ts
    notebook-preview-renderer.ts
    icons.ts
```

本阶段是在这个基础上继续写，不改核心方向。

---

# 1. Phase 2 功能总览

## 1.1 新增能力

```text
1. 节点拖拽
   - 单选拖拽
   - 多选拖拽
   - 拖拽后写回 JSON

2. Tab 新增子节点
   - selected node → new child
   - 自动创建 mindmap edge
   - 自动进入 inline edit

3. Enter 新增兄弟节点
   - selected node sibling
   - 如果 selected 是 root，则新增 root 子节点
   - 自动进入 inline edit

4. 连线编辑
   - 进入连线模式
   - 点击 source，再点击 target 创建 reference edge
   - mindmap edge 暂时只由 Tab/Enter 创建
   - reference edge 两端可见时显示
   - 右键 edge 可删除

5. undo / redo
   - Cmd/Ctrl + Z
   - Cmd/Ctrl + Shift + Z
   - snapshot 模式即可

6. 搜索节点
   - 搜索 title / notebook link / tags
   - 高亮搜索结果
   - Enter 跳到第一个结果

7. 多选 / 框选
   - Cmd/Ctrl 点击 toggle
   - Shift 点击 add
   - Shift + 拖拽空白区域框选
```

---

# 2. 数据模型补充

修改：

```text
src/types/mindmap.ts
```

给 `ProjectedNode` 增加搜索和连线相关状态：

```ts
export interface ProjectedNode {
  id: string;
  sourceNodeId: string;

  kind: NodeKind;
  title: string;

  notebook?: NotebookBinding;
  notebookExists?: boolean;

  worldX: number;
  worldY: number;

  projectedX: number;
  projectedY: number;

  displayWidth: number;
  displayHeight: number;

  detailLevel: NodeDetailLevel;

  isRoot: boolean;
  isFocus: boolean;
  isSelected: boolean;
  isHovered: boolean;
  isAncestorPath: boolean;

  /**
   * Phase 2 新增：
   * 搜索命中高亮。
   */
  isSearchMatch?: boolean;

  /**
   * Phase 2 新增：
   * 连线模式下的 source 节点。
   */
  isConnectionSource?: boolean;

  hasChildren: boolean;
  childrenExpanded: boolean;

  notebook actions live in the context menu
}
```

给 `MindmapEdge` 增加可选样式字段：

```ts
export interface MindmapEdge {
  id: string;
  source: string;
  target: string;
  relation: EdgeRelation;
  type: EdgeType;
  label?: string;

  style?: {
    stroke?: string;
    dashed?: boolean;
  };
}
```

---

# 3. History：undo / redo

创建：

```text
src/core/history.ts
```

```ts
import type { MindmapDocument } from "../types/mindmap";

export class HistoryManager {
  private undoStack: MindmapDocument[] = [];
  private redoStack: MindmapDocument[] = [];

  constructor(private limit = 80) {}

  push(doc: MindmapDocument): void {
    this.undoStack.push(structuredClone(doc));

    if (this.undoStack.length > this.limit) {
      this.undoStack.shift();
    }

    this.redoStack = [];
  }

  undo(current: MindmapDocument): MindmapDocument | null {
    const previous = this.undoStack.pop();
    if (!previous) return null;

    this.redoStack.push(structuredClone(current));
    return structuredClone(previous);
  }

  redo(current: MindmapDocument): MindmapDocument | null {
    const next = this.redoStack.pop();
    if (!next) return null;

    this.undoStack.push(structuredClone(current));
    return structuredClone(next);
  }

  clear(): void {
    this.undoStack = [];
    this.redoStack = [];
  }
}
```

在 `MindmapView` 中加入：

```ts
import { HistoryManager } from "../core/history";

private history = new HistoryManager();

private commitHistory(): void {
  this.history.push(this.store.getDocument());
}

private undo(): void {
  const previous = this.history.undo(this.store.getDocument());
  if (!previous) return;

  this.store.replaceDocument(previous);
  this.renderer?.render();
  this.autosave.schedule();
}

private redo(): void {
  const next = this.history.redo(this.store.getDocument());
  if (!next) return;

  this.store.replaceDocument(next);
  this.renderer?.render();
  this.autosave.schedule();
}
```

在所有结构性操作前调用 `commitHistory()`：

```text
1. 新增节点
2. 删除节点
3. 拖拽开始
4. 创建 edge
5. 删除 edge
6. notebook 转 text
7. text 转 notebook
8. inline title 保存前
9. radial layout 前
```

---

# 4. Search：搜索节点

创建：

```text
src/core/search.ts
```

```ts
import type { MindmapNode } from "../types/mindmap";

export function searchNodes(nodes: MindmapNode[], query: string): MindmapNode[] {
  const q = query.trim().toLowerCase();
  if (!q) return [];

  return nodes.filter((node) => {
    const fields = [
      node.title,
      node.notebook?.link,
      node.notebook?.path,
      node.link,
      ...(node.tags ?? []),
    ];

    return fields
      .filter(Boolean)
      .some((field) => String(field).toLowerCase().includes(q));
  });
}
```

在 `MindmapView` 中增加：

```ts
import { searchNodes } from "../core/search";

private searchQuery = "";
private searchResultIds = new Set<string>();

private updateSearch(query: string): void {
  this.searchQuery = query;

  const results = searchNodes(this.store.getDocument().nodes, query);
  this.searchResultIds = new Set(results.map((node) => node.id));

  this.renderer?.setSearchResultIds(this.searchResultIds);
  this.renderer?.render();
}

private focusFirstSearchResult(): void {
  const firstId = [...this.searchResultIds][0];
  if (!firstId) return;

  this.selection.setOnly(firstId);
  this.renderer?.setLastFocusNodeId(firstId);
  this.renderer?.focusNode(firstId);
  this.renderer?.render();
}
```

在 toolbar 中添加搜索框：

```ts
const searchInput = toolbar.createEl("input", {
  type: "text",
  placeholder: "搜索节点...",
});

searchInput.oninput = () => {
  this.updateSearch(searchInput.value);
};

searchInput.onkeydown = (event) => {
  if (event.key === "Enter") {
    event.preventDefault();
    this.focusFirstSearchResult();
  }
};
```

---

# 5. Semantic Projection 支持搜索高亮与连线 source

修改：

```text
src/core/semantic-projection.ts
```

扩展输入：

```ts
export interface CreateSemanticProjectionExtra {
  searchResultIds?: Set<string>;
  connectionSourceId?: string;
}
```

把函数签名改成：

```ts
export function createSemanticProjection(
  doc: MindmapDocument,
  context: ProjectionContext,
  extra: CreateSemanticProjectionExtra = {},
): SemanticProjection {
  // existing code
}
```

在 projected node push 时增加：

```ts
projectedNodes.push({
  id: node.id,
  sourceNodeId: node.id,

  kind: node.kind,
  title: node.title,
  notebook: node.notebook,

  worldX: node.x,
  worldY: node.y,

  projectedX: node.x,
  projectedY: node.y,

  displayWidth: visual.width,
  displayHeight: visual.height,

  detailLevel: detail,

  isRoot,
  isFocus,
  isSelected,
  isHovered,
  isAncestorPath,

  isSearchMatch: extra.searchResultIds?.has(node.id) ?? false,
  isConnectionSource: extra.connectionSourceId === node.id,

  hasChildren: children.length > 0,
  childrenExpanded,

  notebook actions live in the context menu
});
```

---

# 6. Store 扩展：边和批量节点移动

修改：

```text
src/core/document-store.ts
```

新增：

```ts
import type { MindmapEdge, EdgeRelation, EdgeType } from "../types/mindmap";
import { createId } from "./id";
```

新增方法：

```ts
addEdge(args: {
  source: string;
  target: string;
  relation: EdgeRelation;
  type?: EdgeType;
}): void {
  if (args.source === args.target) return;

  const exists = this.doc.edges.some(
    (edge) =>
      edge.source === args.source &&
      edge.target === args.target &&
      edge.relation === args.relation,
  );

  if (exists) return;

  /**
   * mindmap 主树不允许多父节点。
   */
  if (args.relation === "mindmap") {
    const alreadyHasParent = this.doc.edges.some(
      (edge) => edge.relation === "mindmap" && edge.target === args.target,
    );

    if (alreadyHasParent) return;
  }

  this.doc.edges.push({
    id: createId("edge"),
    source: args.source,
    target: args.target,
    relation: args.relation,
    type: args.type ?? "curve",
  });

  this.emit();
}

deleteEdge(id: string): void {
  this.doc.edges = this.doc.edges.filter((edge) => edge.id !== id);
  this.emit();
}

updateNodePositions(moves: Array<{ id: string; x: number; y: number }>): void {
  const moveMap = new Map(moves.map((move) => [move.id, move]));

  for (const node of this.doc.nodes) {
    const move = moveMap.get(node.id);
    if (!move) continue;

    node.x = move.x;
    node.y = move.y;
  }

  this.emit();
}
```

保留已有：

```ts
addMindmapEdge(source, target)
```

但内部可以改成调用：

```ts
this.addEdge({ source, target, relation: "mindmap", type: "curve" });
```

---

# 7. 节点拖拽

## 7.1 Renderer 接口扩展

修改：

```text
src/renderer/svg-mindmap-renderer.ts
```

构造参数增加：

```ts
onBeforeNodeDragStart: () => void;
onNodeDragEnd: () => void;
onNodesMove: (moves: Array<{ id: string; x: number; y: number }>) => void;
```

完整 options 片段：

```ts
private options: {
  app: App;
  container: HTMLElement;
  sourcePath: string;

  getDocument: () => MindmapDocument;
  getSelectedNodeIds: () => string[];

  onViewportChange: (x: number, y: number, zoom: number) => void;
  onSelectNode: (id: string, mode: "replace" | "toggle" | "add") => void;
  onToggleTree: (id: string) => void;
  notebook actions moved to context menu
  onInlineTitleCommit: (id: string, title: string) => Promise<void>;
  onContextMenu: (id: string, x: number, y: number) => void;

  onBeforeNodeDragStart: () => void;
  onNodesMove: (moves: Array<{ id: string; x: number; y: number }>) => void;
  onNodeDragEnd: () => void;
}
```

---

## 7.2 Projected Node Renderer 增加 drag

修改：

```text
src/renderer/projected-node-renderer.ts
```

options 增加：

```ts
getSelectedNodeIds: () => string[];
onBeforeNodeDragStart: () => void;
onNodesMove: (moves: Array<{ id: string; x: number; y: number }>) => void;
onNodeDragEnd: () => void;
```

renderProjectedNodes 函数参数变成：

```ts
export function renderProjectedNodes(args: {
  app: App;
  nodeLayer: d3.Selection<SVGGElement, unknown, null, undefined>;
  nodes: ProjectedNode[];
  transform: ViewTransform;
  sourcePath: string;

  getSelectedNodeIds: () => string[];

  onSelectNode: (id: string, mode: "replace" | "toggle" | "add") => void;
  onHoverNode: (id: string) => void;
  onLeaveNode: () => void;
  onToggleTree: (id: string) => void;
  notebook actions moved to context menu
  onStartInlineEdit: (
    node: ProjectedNode,
    rect: { x: number; y: number; width: number; height: number },
  ) => void;
  onContextMenu: (id: string, x: number, y: number) => void;

  onBeforeNodeDragStart: () => void;
  onNodesMove: (moves: Array<{ id: string; x: number; y: number }>) => void;
  onNodeDragEnd: () => void;
}): void
```

给 `merged` 添加 drag：

```ts
const dragBehavior = d3
  .drag<SVGGElement, ProjectedNode>()
  .on("start", (event, node) => {
    event.sourceEvent?.stopPropagation();

    args.onBeforeNodeDragStart();

    const selectedIds = args.getSelectedNodeIds();

    if (!selectedIds.includes(node.id)) {
      args.onSelectNode(node.id, "replace");
    }
  })
  .on("drag", (event, node) => {
    const selectedIds = args.getSelectedNodeIds();
    const movingIds = selectedIds.includes(node.id) ? selectedIds : [node.id];

    const projectedMap = new Map(args.nodes.map((item) => [item.id, item]));

    /**
     * event.dx / event.dy 是屏幕像素。
     * 要转换成世界坐标 delta。
     */
    const dxWorld = event.dx / args.transform.k;
    const dyWorld = event.dy / args.transform.k;

    const moves = movingIds
      .map((id) => {
        const item = projectedMap.get(id);
        if (!item) return null;

        return {
          id,
          x: item.worldX + dxWorld,
          y: item.worldY + dyWorld,
        };
      })
      .filter(Boolean) as Array<{ id: string; x: number; y: number }>;

    args.onNodesMove(moves);
  })
  .on("end", () => {
    args.onNodeDragEnd();
  });

merged.call(dragBehavior);
```

注意：

```text
drag 写回 node.x / node.y。
不是 projectedX / projectedY。
```

---

## 7.3 SvgMindmapRenderer 接入 drag options

在 `renderProjectedNodes` 调用中加入：

```ts
getSelectedNodeIds: this.options.getSelectedNodeIds,

onBeforeNodeDragStart: this.options.onBeforeNodeDragStart,
onNodesMove: this.options.onNodesMove,
onNodeDragEnd: this.options.onNodeDragEnd,
```

---

## 7.4 MindmapView 接入拖拽

在 `new SvgMindmapRenderer({...})` 中加入：

```ts
onBeforeNodeDragStart: () => {
  this.commitHistory();
},

onNodesMove: (moves) => {
  this.store.updateNodePositions(moves);
  this.renderer?.render();
  this.autosave.schedule();
},

onNodeDragEnd: () => {
  this.autosave.schedule();
},
```

---

# 8. Tab 新增子节点 / Enter 新增兄弟节点

## 8.1 新建图结构工具

创建：

```text
src/core/tree-editing.ts
```

```ts
import type { MindmapDocument, MindmapNode } from "../types/mindmap";
import { buildHierarchy } from "./hierarchy";
import { createId } from "./id";
import { DEFAULT_NODE_HEIGHT, DEFAULT_NODE_WIDTH, DEFAULT_TEXT_NODE_TITLE } from "../constants";

export function createTextNodeNearParent(parent: MindmapNode): MindmapNode {
  return {
    id: createId("node"),
    kind: "text",
    title: DEFAULT_TEXT_NODE_TITLE,
    x: parent.x + 220,
    y: parent.y + 80,
    width: DEFAULT_NODE_WIDTH,
    height: DEFAULT_NODE_HEIGHT,
    treeControl: "auto",
  };
}

export function findParentId(doc: MindmapDocument, nodeId: string): string | undefined {
  const hierarchy = buildHierarchy(doc);
  return hierarchy.parentById.get(nodeId);
}

export function findRootId(doc: MindmapDocument): string | undefined {
  return buildHierarchy(doc).rootId;
}
```

---

## 8.2 MindmapView 新增方法

在 `MindmapView` 中加入：

```ts
import { createTextNodeNearParent, findParentId, findRootId } from "../core/tree-editing";
```

新增：

```ts
private addChildNode(): void {
  const selectedId = this.selection.getIds()[0];
  if (!selectedId) return;

  const doc = this.store.getDocument();
  const parent = doc.nodes.find((node) => node.id === selectedId);
  if (!parent) return;

  this.commitHistory();

  const child = createTextNodeNearParent(parent);

  this.store.addNode(child);
  this.store.addEdge({
    source: parent.id,
    target: child.id,
    relation: "mindmap",
    type: "curve",
  });

  this.selection.setOnly(child.id);
  this.renderer?.setLastFocusNodeId(child.id);
  this.renderer?.render();
  this.renderer?.focusNode(child.id);
  this.autosave.schedule();

  /**
   * MVP 可以先不自动打开 inline editor。
   * 如果 renderer 提供 startInlineEditById，可在这里调用。
   */
}

private addSiblingNode(): void {
  const selectedId = this.selection.getIds()[0];
  if (!selectedId) return;

  const doc = this.store.getDocument();
  const selected = doc.nodes.find((node) => node.id === selectedId);
  if (!selected) return;

  const parentId = findParentId(doc, selectedId) ?? findRootId(doc);
  if (!parentId) return;

  const parent = doc.nodes.find((node) => node.id === parentId);
  if (!parent) return;

  this.commitHistory();

  const sibling = {
    ...createTextNodeNearParent(parent),
    x: selected.x + 40,
    y: selected.y + 100,
  };

  this.store.addNode(sibling);
  this.store.addEdge({
    source: parent.id,
    target: sibling.id,
    relation: "mindmap",
    type: "curve",
  });

  this.selection.setOnly(sibling.id);
  this.renderer?.setLastFocusNodeId(sibling.id);
  this.renderer?.render();
  this.renderer?.focusNode(sibling.id);
  this.autosave.schedule();
}
```

---

## 8.3 注册键盘事件

在 `MindmapView.onOpen()` 或 `renderView()` 中注册容器键盘事件。

推荐在 `renderView()` 的 canvas 上加：

```ts
canvas.tabIndex = 0;
canvas.addEventListener("keydown", (event) => this.handleCanvasKeydown(event));
```

新增方法：

```ts
private handleCanvasKeydown(event: KeyboardEvent): void {
  const target = event.target as HTMLElement;

  /**
   * 如果正在 input 里编辑，交给 input 自己处理。
   */
  if (target.closest("input, textarea")) return;

  if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "z") {
    event.preventDefault();

    if (event.shiftKey) {
      this.redo();
    } else {
      this.undo();
    }

    return;
  }

  if (event.key === "Tab") {
    event.preventDefault();
    this.addChildNode();
    return;
  }

  if (event.key === "Enter") {
    event.preventDefault();
    this.addSiblingNode();
    return;
  }

  if (event.key === "Delete" || event.key === "Backspace") {
    event.preventDefault();
    this.deleteSelectedNodes();
    return;
  }

  if (event.key === "Escape") {
    this.selection.clear();
    this.renderer?.render();
  }
}
```

新增删除选中节点：

```ts
private deleteSelectedNodes(): void {
  const ids = this.selection.getIds();
  if (ids.length === 0) return;

  this.commitHistory();

  for (const id of ids) {
    this.store.deleteNode(id);
  }

  this.selection.clear();
  this.renderer?.render();
  this.autosave.schedule();
}
```

---

# 9. 连线编辑

## 9.1 交互选择

MVP 不要做“拖线创建边”，先做更可靠的两步点击：

```text
1. 点击工具栏“连线模式”
2. 点击 source 节点
3. 点击 target 节点
4. 创建 reference edge
5. 自动退出 source，保留连线模式
```

原因：

```text
当前右侧 + / - 已经占用结构按钮。
不要再引入右侧圆点。
```

---

## 9.2 MindmapView 增加 connection mode 状态

```ts
private connectionMode = false;
private connectionSourceId: string | undefined;
```

Toolbar 添加按钮：

```ts
const connectButton = toolbar.createEl("button", { text: "连线" });

connectButton.onclick = () => {
  this.connectionMode = !this.connectionMode;
  this.connectionSourceId = undefined;
  connectButton.toggleClass("is-active", this.connectionMode);
  this.renderer?.setConnectionState({
    enabled: this.connectionMode,
    sourceId: this.connectionSourceId,
  });
  this.renderer?.render();
};
```

新增方法：

```ts
private handleNodeSelectedForConnection(id: string): boolean {
  if (!this.connectionMode) return false;

  if (!this.connectionSourceId) {
    this.connectionSourceId = id;
    this.renderer?.setConnectionState({
      enabled: true,
      sourceId: id,
    });
    this.renderer?.render();
    return true;
  }

  if (this.connectionSourceId === id) {
    this.connectionSourceId = undefined;
    this.renderer?.setConnectionState({
      enabled: true,
      sourceId: undefined,
    });
    this.renderer?.render();
    return true;
  }

  this.commitHistory();

  this.store.addEdge({
    source: this.connectionSourceId,
    target: id,
    relation: "reference",
    type: "curve",
  });

  this.connectionSourceId = undefined;

  this.renderer?.setConnectionState({
    enabled: true,
    sourceId: undefined,
  });

  this.renderer?.render();
  this.autosave.schedule();

  return true;
}
```

在 renderer `onSelectNode` 之前拦截：

```ts
onSelectNode: (id, mode) => {
  if (this.handleNodeSelectedForConnection(id)) return;

  if (mode === "replace") this.selection.setOnly(id);
  if (mode === "toggle") this.selection.toggle(id);
  if (mode === "add") this.selection.add(id);
},
```

---

## 9.3 SvgMindmapRenderer 支持 connection state

增加成员：

```ts
private connectionEnabled = false;
private connectionSourceId: string | undefined;
```

新增方法：

```ts
setConnectionState(state: { enabled: boolean; sourceId?: string }): void {
  this.connectionEnabled = state.enabled;
  this.connectionSourceId = state.sourceId;
}
```

`createSemanticProjection` 调用改为：

```ts
const projection = createSemanticProjection(
  doc,
  {
    zoom: transform.k,
    viewportWorldRect: this.getViewportWorldRect(),
    selectedNodeIds: this.options.getSelectedNodeIds(),
    hoveredNodeId: this.hoveredNodeId,
    lastFocusNodeId: this.lastFocusNodeId,
  },
  {
    searchResultIds: this.searchResultIds,
    connectionSourceId: this.connectionSourceId,
  },
);
```

---

## 9.4 Edge Renderer 支持右键删除 edge

修改：

```text
src/renderer/projected-edge-renderer.ts
```

函数参数增加：

```ts
onEdgeContextMenu?: (id: string, x: number, y: number) => void;
```

签名：

```ts
export function renderProjectedEdges(args: {
  edgeLayer: d3.Selection<SVGGElement, unknown, null, undefined>;
  nodes: ProjectedNode[];
  edges: ProjectedEdge[];
  onEdgeContextMenu?: (id: string, x: number, y: number) => void;
}): void
```

在 path 上增加：

```ts
entered
  .merge(selection)
  .on("contextmenu", (event, edge) => {
    event.preventDefault();
    event.stopPropagation();
    args.onEdgeContextMenu?.(edge.id, event.clientX, event.clientY);
  });
```

---

## 9.5 SvgMindmapRenderer 传递 edge context menu

构造参数增加：

```ts
onEdgeContextMenu: (id: string, x: number, y: number) => void;
```

调用：

```ts
renderProjectedEdges({
  edgeLayer: this.edgeWorldLayer,
  nodes: projection.nodes,
  edges: projection.edges,
  onEdgeContextMenu: this.options.onEdgeContextMenu,
});
```

---

## 9.6 MindmapView 删除 edge 菜单

Renderer options 增加：

```ts
onEdgeContextMenu: (id, x, y) => {
  this.openEdgeContextMenu(id, x, y);
},
```

新增：

```ts
private openEdgeContextMenu(edgeId: string, x: number, y: number): void {
  const menu = new Menu();

  menu.addItem((item) => {
    item
      .setTitle("删除连线")
      .setIcon("trash")
      .onClick(() => {
        this.commitHistory();
        this.store.deleteEdge(edgeId);
        this.renderer?.render();
        this.autosave.schedule();
      });
  });

  menu.showAtPosition({ x, y });
}
```

---

# 10. 框选

## 10.1 几何工具

创建：

```text
src/core/geometry.ts
```

```ts
import type { MindmapNode, Rect } from "../types/mindmap";

export function normalizeRect(x1: number, y1: number, x2: number, y2: number): Rect {
  return {
    x: Math.min(x1, x2),
    y: Math.min(y1, y2),
    width: Math.abs(x2 - x1),
    height: Math.abs(y2 - y1),
  };
}

export function rectIntersects(a: Rect, b: Rect): boolean {
  return !(
    a.x + a.width < b.x ||
    b.x + b.width < a.x ||
    a.y + a.height < b.y ||
    b.y + b.height < a.y
  );
}

export function nodeWorldRect(node: MindmapNode): Rect {
  return {
    x: node.x,
    y: node.y,
    width: node.width,
    height: node.height,
  };
}
```

---

## 10.2 SvgMindmapRenderer 添加框选层

成员：

```ts
private overlayScreenLayer!: d3.Selection<SVGGElement, unknown, null, undefined>;
private selecting = false;
private selectionStartWorld: { x: number; y: number } | null = null;
```

mount 中：

```ts
this.overlayScreenLayer = this.svg.append("g").attr("class", "overlay-screen-layer");

this.overlayScreenLayer
  .append("rect")
  .attr("class", "selection-box")
  .style("display", "none");
```

options 增加：

```ts
onBoxSelect: (rect: { x: number; y: number; width: number; height: number }) => void;
```

在 mount 中添加事件：

```ts
this.svg.on("mousedown.boxselect", (event) => {
  if (!event.shiftKey) return;
  if ((event.target as Element).closest(".mindmap-node")) return;

  event.preventDefault();

  const transform = d3.zoomTransform(this.svg.node()!);
  const [x, y] = transform.invert(d3.pointer(event, this.svg.node()));

  this.selecting = true;
  this.selectionStartWorld = { x, y };
});

this.svg.on("mousemove.boxselect", (event) => {
  if (!this.selecting || !this.selectionStartWorld) return;

  const transform = d3.zoomTransform(this.svg.node()!);
  const [worldX, worldY] = transform.invert(d3.pointer(event, this.svg.node()));

  const startScreen = [
    this.selectionStartWorld.x * transform.k + transform.x,
    this.selectionStartWorld.y * transform.k + transform.y,
  ];

  const currentScreen = d3.pointer(event, this.svg.node());

  const x = Math.min(startScreen[0], currentScreen[0]);
  const y = Math.min(startScreen[1], currentScreen[1]);
  const width = Math.abs(currentScreen[0] - startScreen[0]);
  const height = Math.abs(currentScreen[1] - startScreen[1]);

  this.overlayScreenLayer
    .select<SVGRectElement>("rect.selection-box")
    .style("display", null)
    .attr("x", x)
    .attr("y", y)
    .attr("width", width)
    .attr("height", height);
});

this.svg.on("mouseup.boxselect", (event) => {
  if (!this.selecting || !this.selectionStartWorld) return;

  const transform = d3.zoomTransform(this.svg.node()!);
  const [x2, y2] = transform.invert(d3.pointer(event, this.svg.node()));

  const x1 = this.selectionStartWorld.x;
  const y1 = this.selectionStartWorld.y;

  const rect = {
    x: Math.min(x1, x2),
    y: Math.min(y1, y2),
    width: Math.abs(x2 - x1),
    height: Math.abs(y2 - y1),
  };

  this.options.onBoxSelect(rect);

  this.selecting = false;
  this.selectionStartWorld = null;

  this.overlayScreenLayer
    .select<SVGRectElement>("rect.selection-box")
    .style("display", "none");
});
```

注意：如果 d3 zoom 与 boxselect 冲突，第一版用 Shift 框选即可，用户按 Shift 时不 pan。

---

## 10.3 MindmapView 实现框选

Renderer options 增加：

```ts
onBoxSelect: (rect) => {
  const ids = this.store
    .getDocument()
    .nodes
    .filter((node) => rectIntersects(rect, nodeWorldRect(node)))
    .map((node) => node.id);

  this.selection.clear();

  for (const id of ids) {
    this.selection.add(id);
  }

  this.renderer?.render();
},
```

需要 import：

```ts
import { rectIntersects, nodeWorldRect } from "../core/geometry";
```

---

# 11. 搜索状态接入 Renderer

在 `SvgMindmapRenderer` 增加：

```ts
private searchResultIds = new Set<string>();

setSearchResultIds(ids: Set<string>): void {
  this.searchResultIds = new Set(ids);
}
```

在 semantic projection 调用中传入：

```ts
{
  searchResultIds: this.searchResultIds,
  connectionSourceId: this.connectionSourceId,
}
```

---

# 12. Node Renderer 增加 class

在 `projected-node-renderer.ts` 中：

```ts
group.classed("is-search-match", Boolean(node.isSearchMatch));
group.classed("is-connection-source", Boolean(node.isConnectionSource));
```

CSS 增加：

```css
.mindmap-node.is-search-match .mindmap-node-bg {
  stroke: var(--text-accent);
  stroke-width: 3px;
}

.mindmap-node.is-connection-source .mindmap-node-bg {
  stroke: var(--interactive-accent);
  stroke-width: 3px;
  stroke-dasharray: 4 3;
}

.semantic-mindmap-toolbar button.is-active {
  background: var(--interactive-accent);
  color: var(--text-on-accent);
}

.selection-box {
  fill: var(--interactive-accent);
  fill-opacity: 0.08;
  stroke: var(--interactive-accent);
  stroke-width: 1.5px;
  stroke-dasharray: 4 4;
  pointer-events: none;
}
```

---

# 13. Renderer Options 汇总

最终 `SvgMindmapRenderer` 构造 options 应包含：

```ts
{
  app: App;
  container: HTMLElement;
  sourcePath: string;

  getDocument: () => MindmapDocument;
  getSelectedNodeIds: () => string[];

  onViewportChange: (x: number, y: number, zoom: number) => void;

  onSelectNode: (id: string, mode: "replace" | "toggle" | "add") => void;

  onToggleTree: (id: string) => void;
  notebook actions moved to context menu

  onInlineTitleCommit: (id: string, title: string) => Promise<void>;

  onContextMenu: (id: string, x: number, y: number) => void;
  onEdgeContextMenu: (id: string, x: number, y: number) => void;

  onBeforeNodeDragStart: () => void;
  onNodesMove: (moves: Array<{ id: string; x: number; y: number }>) => void;
  onNodeDragEnd: () => void;

  onBoxSelect: (rect: Rect) => void;
}
```

---

# 14. MindmapView Renderer 初始化汇总

在 `MindmapView.renderView()` 中，`new SvgMindmapRenderer` 最终类似：

```ts
this.renderer = new SvgMindmapRenderer({
  app: this.app,
  container: canvas,
  sourcePath: this.sourceFile?.path ?? "",

  getDocument: () => this.store.getDocument(),
  getSelectedNodeIds: () => this.selection.getIds(),

  onViewportChange: (x, y, zoom) => {
    this.store.setViewport(x, y, zoom);
    this.autosave.schedule();
  },

  onSelectNode: (id, mode) => {
    if (this.handleNodeSelectedForConnection(id)) return;

    if (mode === "replace") this.selection.setOnly(id);
    if (mode === "toggle") this.selection.toggle(id);
    if (mode === "add") this.selection.add(id);
  },

  onToggleTree: (id) => {
    this.commitHistory();
    this.store.toggleTreeControl(id);
    this.renderer?.render();
    this.autosave.schedule();
  },

  notebook actions moved to context menu
    void this.handleNotebookExpand(id);
  },

  onInlineTitleCommit: async (id, title) => {
    this.commitHistory();
    await this.handleInlineTitleCommit(id, title);
  },

  onContextMenu: (id, x, y) => {
    this.openContextMenu(id, x, y);
  },

  onEdgeContextMenu: (id, x, y) => {
    this.openEdgeContextMenu(id, x, y);
  },

  onBeforeNodeDragStart: () => {
    this.commitHistory();
  },

  onNodesMove: (moves) => {
    this.store.updateNodePositions(moves);
    this.renderer?.render();
    this.autosave.schedule();
  },

  onNodeDragEnd: () => {
    this.autosave.schedule();
  },

  onBoxSelect: (rect) => {
    const ids = this.store
      .getDocument()
      .nodes
      .filter((node) => rectIntersects(rect, nodeWorldRect(node)))
      .map((node) => node.id);

    this.selection.clear();

    for (const id of ids) {
      this.selection.add(id);
    }

    this.renderer?.render();
  },
});
```

---

# 15. styles.css 追加

```css
.semantic-mindmap-toolbar input[type="text"] {
  min-width: 180px;
  padding: 4px 8px;
  border: 1px solid var(--background-modifier-border);
  border-radius: 6px;
  background: var(--background-primary);
  color: var(--text-normal);
}

.semantic-mindmap-toolbar button.is-active {
  background: var(--interactive-accent);
  color: var(--text-on-accent);
}

.mindmap-node.is-search-match .mindmap-node-bg {
  stroke: var(--text-accent);
  stroke-width: 3px;
}

.mindmap-node.is-connection-source .mindmap-node-bg {
  stroke: var(--interactive-accent);
  stroke-width: 3px;
  stroke-dasharray: 4 3;
}

.selection-box {
  fill: var(--interactive-accent);
  fill-opacity: 0.08;
  stroke: var(--interactive-accent);
  stroke-width: 1.5px;
  stroke-dasharray: 4 4;
  pointer-events: none;
}

.mindmap-edge {
  cursor: pointer;
}

.mindmap-edge:hover {
  stroke: var(--interactive-accent);
}
```

---

# 16. Phase 2 验收标准

完成 Phase 2 后必须满足：

```text
1. 可以拖拽单个节点。
2. 多选后拖拽任一选中节点，所有选中节点一起移动。
3. 拖拽写回 node.x / node.y。
4. 拖拽产生一次 undo 快照，不是每帧一个快照。
5. Cmd/Ctrl + Z 可以 undo。
6. Cmd/Ctrl + Shift + Z 可以 redo。
7. Tab 可以给 selected node 新增子节点。
8. Enter 可以给 selected node 新增兄弟节点。
9. 新增子节点自动创建 mindmap edge。
10. 新增兄弟节点自动创建 mindmap edge。
11. Delete / Backspace 删除选中节点。
12. 搜索框可以搜索 title / notebook link / tags。
13. Enter 可以跳转到第一个搜索结果。
14. 搜索结果节点高亮。
15. Cmd/Ctrl 点击节点可以 toggle 多选。
16. Shift 点击节点可以 add 多选。
17. Shift + 拖拽空白区域可以框选节点。
18. 连线模式下，点击两个节点可以创建 reference edge。
19. 连线模式创建的 reference edge 样式为虚线。
20. mindmap edge 和 reference edge 样式不同。
21. 右键 edge 可以删除 edge。
22. 两端可见的 edge 仍然必须显示。
23. + / - 仍只负责子树展开/收起。
24. 双下箭头仍只负责 text → notebook / notebook 展开。
25. 不能重新引入右侧小圆点连接控件。
```

---

# 17. Phase 2 禁止事项

执行模型必须遵守：

```text
1. 不要把 + / - 用作新增节点。
2. 不要恢复右侧圆点连接控件。
3. 不要用拖线创建 edge 作为第一版连接方案。
4. 不要让 reference edge 参与 radial layout。
5. 不要允许 mindmap 主树多父节点。
6. 不要每一帧拖拽都 push history。
7. 不要让框选写回 JSON。
8. 不要让搜索改变 JSON。
9. 不要让 edge 一端不可见时强行显示。
10. 不要让节点内容随 zoom 连续缩放。
```

---

# 18. 推荐实现顺序

严格按下面顺序执行：

```text
1. 实现 history.ts
2. MindmapView 接入 undo / redo
3. document-store.ts 增加 addEdge/deleteEdge/updateNodePositions
4. 实现 search.ts
5. MindmapView 添加搜索框
6. semantic-projection.ts 增加 search / connection 状态
7. renderer 增加 searchResultIds / connectionSourceId
8. projected-node-renderer 添加拖拽
9. MindmapView 接入拖拽
10. 实现 tree-editing.ts
11. MindmapView 接入 Tab / Enter / Delete
12. 实现连线模式：两步点击创建 reference edge
13. projected-edge-renderer 支持右键
14. MindmapView 添加 edge context menu
15. 实现 geometry.ts
16. SvgMindmapRenderer 添加 Shift 框选
17. MindmapView 接入 box select
18. 补 CSS
19. 全量回归测试
```

---

# 19. Phase 2 完成后的项目状态

完成 Phase 2 后，插件已经具备基础脑图软件能力：

```text
- text 节点一句话编辑
- notebook 节点笔记展开
- 语义缩放
- 中心布局
- 拖拽节点
- 新增子节点 / 兄弟节点
- 多选 / 框选
- 搜索
- reference 连线
- undo / redo
```

Phase 3 再做：

```text
1. 自动从 Markdown 标题生成脑图
2. 自动从 backlinks 生成局部知识地图
3. notebook 选择器
4. 设置页：notebook 文件夹
5. 更好的 radial layout 避让算法
6. 大图性能优化
7. 局部 tile index，仅作为性能优化，不做语义聚合
```
